# Caitlyn development handoff

Latest checkpoint: **2026-09-19 — README rewritten and `caitlyn-3.0` merged/pushed to `main` at `4d76570`; four-image GitHub release passed. Server updater corrected for TrueNAS command checking; first rollout restored the previous version after its startup deadline. Stronger preflight prepared for the next release. Original database retained; channel settings still need the owner's choice.**

Updated: 2026-09-14. Sanitized, signed feature-branch checkpoints: modernization `09ac21d`, resilience `3244bbb`, private logging `ed3c2ea`, birthday recovery `84c16bd`, X adapter `50703ee`, self-hosted X/feature configuration/clean footer `2d26b3f`, and TikTok/presentation `870dfbb`. Current integration branch: `caitlyn-3.0` (formerly `caitlyn-2.0`). Active feature branch: `codex/social-media-replacement`. See the publication checkpoint below before publishing or merging any older local branch.

The TypeScript integration was already complete at `c468729` (`docs: record Caitlyn 2.0 follow-ups`). The original integration plan under `docs/superpowers/` describes that completed migration; use this file and [the task list](.todo) for current progress. Modernization, resilience, private Discord logging, and birthday recovery are committed and integrated locally.

## README and coordinated automatic deployment (2026-09-19)

The owner explicitly approved completing automatic deployment, committing deployment changes, and merging `caitlyn-3.0` into `main`, with a full plain-language README rewrite **before** the merge. The README now focuses on features, actual command names/arguments, social limits and a short quick start. Detailed environment/database instructions moved to `docs/setup.md`; no unsupported MIT licence claim was retained. Supporting documents remain under `docs/`.

The new workflow checks the source, builds/publishes all four AMD64 images at commit-SHA tags, records exact digests and container package inventories, and only then creates a complete `main-<SHA>` GitHub release. The mainframe's Python updater uses the existing TrueNAS API client and checks completed releases every five minutes, requires the current main SHA and approved migration fingerprint, preloads all images, checks the original database read-only, preserves existing service configuration, waits for startup and registers guild commands. Startup/registration failure restores the previous app/commands; unknown in-flight TrueNAS results or failed rollback pause for operator review. No automatic SQL migrations, global command registration, database replacement or Docker pruning.

The server updater is installed under root-owned `/root/.config/caitlyn-release-updater`, separate from the apps-owned runtime mounts; its private configuration approves migration fingerprint `895d3972c9b0d440340add3e2b05805e092cb3cedd3e8067acaf43587900bdc4` (unchanged migrations 001–016, with the original target already verified). This is an installed project script/configuration, **not a host package installation**. Existing GHCR credentials can read the bot image; no credentials were printed, copied to GitHub or added to source. TrueNAS task **2** runs every five minutes; other tasks are unchanged. See [automatic deployment](automatic-deployment.md) for state, failure handling and package inventories.

Signed commit `4d765705b99f8000b2d47334c73edafd5425ea6f` was fast-forwarded through `caitlyn-3.0` to `main`; all three safe branches were pushed atomically using the existing SSH identity. The HTTPS attempt was refused for missing workflow scope and changed no remote refs. No credentials/scopes were changed. [GitHub Actions run 35401194037](https://github.com/RazeViana/Caitlyn/actions/runs/35401194037) passed and published all four images plus release/package inventories. Every installed package version/path matches the initial server-image inventory; no host packages were installed.

The first live updater attempts stopped the app and paused before applying configuration. Additional safe diagnostics identified TrueNAS sudo command checking killing `midclt` when verifying the 4,099-byte Compose argument. Each stopped app was restored only after checking completed jobs and verifying its configuration still matched the saved original. Pending states were preserved privately for review. The fix uses TrueNAS's already installed Python API client over its local socket, keeping configuration off command-line arguments without changing sudo policy. Known job-status reads can be retried for five minutes; mutation requests are not repeated blindly, and exact job IDs/stages are logged. A later preflight command timeout left the running app untouched; no database failure or data change was reported.

The native-client update then applied all new images successfully, but the new bot had no completed-startup marker within two minutes. The updater restored the saved old images, checked readiness and registered old guild commands; rollback completed at **22:51:49 UTC on September 18**, with `4d76570` blocked from further automatic attempts. Original database/secrets/settings are unchanged. Host inspection saw a bot process waiting on I/O temporarily; all ZFS pools report healthy, so the cause of that startup delay is not established. The next release preflight imports the full inert bot entry point before stopping the working version and allows 90 seconds for that read-only check. This checkpoint does not claim the next release has deployed; verify its CI, updater state and live startup afterward.

The full working tree passed 447 tests plus one optional PostgreSQL skip before the additional preflight assertion. The final exact staged snapshot passed **407 tests plus one optional PostgreSQL skip**, typecheck, lint and build. The updater's **26 Python tests** passed both locally and on TrueNAS Python 3.11, covering stopped/busy services, interrupted updates, bad image sets, metadata/inventory binding, download/preflight failures, rollback, registration failure, private files, safe status retries and the native API/large-argument regression. Preserve and exclude all separately parked Reddit source/tests/documentation hunks from the release.

## Historical mainframe activation with original database (2026-09-18)

The owner approved deployment and explicitly selected the original mainframe database. TrueNAS app update **156789 succeeded**; Caitlyn reported **online at 21:45:56 UTC** and startup complete at 21:45:57 UTC. Production bot container: `ix-caitlyn-caitlyn-1`, with separate healthy broker/FxEmbed containers under the `caitlyn` app and the existing healthy `caitlyn-discord-bot-db` app. Local bot/broker processes are stopped; the local FxEmbed test container remains separate and cannot log into Discord. Do not start a second bot with the same token.

Deployment uses published `bcde78a` plus reviewed, currently uncommitted deployment overlays. Exact AMD64 images, protected runtime paths, backup/restore evidence, migrations and rollback precautions are in [mainframe deployment](mainframe-deployment.md). The old mutable-image/Watchtower setup was replaced with pinned local image IDs and private service mounts. X authentication reloads from an owner-only server file on each restart without the Mac or 1Password; expired sessions still need owner renewal. AI is off; original PostgreSQL database `caitlyn` on port 5433 is in use, not local `caitlyn_test`. `main` remains unchanged. No additional commit, merge or push occurred during deployment; preserve the separately parked Reddit changes.

Original database protection: pre-start ZFS snapshot plus private custom-format dump; restored/tested in a temporary local database that was subsequently removed. Migrations **010–016** committed with locked pre/post row-count and UTC-normalized fingerprint checks. Preserved 10 birthdays, 1,319 daily activity rows, 34 messages, 16 user activity rows and 1,914 voice sessions. No local testing history or counters were copied. Never replay migration 002. No birthday was due on the checked deployment day or following day. Existing 14 guild commands, including the Instagram `/social` description, were verified; no command registration change was needed.

Live mainframe extraction passed X video, restricted X video, Instagram photo and reel. After restart, authenticated X passed again; the reel compressed to 905,859 bytes under a diagnostic 1 MiB cap, with no media omitted. **The long TikTok sample timed out during compression** and returned no attachment; tune mainframe long-video performance in a separate change while retaining complete-output checks and bounded resource use. The first restart refused an orphaned temporary worker IPC volume; after verifying its label and no attached containers, only that exact volume was removed. Subsequent checks left no owned worker resources, and a second clean stop/start passed. No live Discord test posts were sent; owner playback/layout acceptance remains outstanding.

Verification: full working-tree check passed 443 tests plus one optional PostgreSQL skip, typecheck, lint and build; all 10 targeted deployment tests passed again. Clean release plus initial runtime tests passed 399 tests plus one skip; 10 offline Instagram fixtures passed in the actual server image. Non-root image startup imports passed. No packages were installed on macOS or the TrueNAS host. New container-only software and every installed package/version/path are listed in [the installation inventory](mainframe-installations.md).

The original database has **no saved private log channel/levels or social channel opt-ins**. Owner has been asked whether to copy only these settings from the local database; do not assume the unanswered recommended option is consent. Until configured, console logging runs but Discord forwarding has no destination, and social channels are not enabled. Either obtain that settings-transfer choice or use `/setup logs`, `/logs levels` and `/social enable` in Discord. New collection logs remain metadata-only, never message text.

## Historical published logging release and mainframe review (2026-09-18)

Signed commit `bcde78a7f664900ddaec4d1c6ad8259cb2d7fa3c` (`feat: add plain-language activity logs and connection recovery`) was fast-forwarded into `caitlyn-3.0`. An atomic push published both that branch and `codex/social-media-replacement`, including the previous Instagram checkpoint. Both GitHub tips were verified at this commit; `main` is unchanged. The exact release passed 392 tests plus one optional PostgreSQL skip, typecheck, lint and build. Parked Reddit files/hunks remain uncommitted and were not published.

Read-only SSH over Tailscale confirmed an x86-64 TrueNAS/Docker host. Both the existing `caitlyn` app and original `caitlyn-discord-bot-db` app are stopped. The old app uses `latest` with Watchtower opt-in; the repository builds only the bot image on `main` pushes. The server's old environment enables AI, lacks media configuration, and needs tighter secret-file access. No secrets were printed. The original database is offline, so its schema/data have not been assessed. See [the deployment assessment and cutover checklist](mainframe-deployment.md) for AMD64 packaging, broker/backend supervision, protected X authentication, database selection and migration safeguards.

Nothing was installed or changed on the mainframe; no local bot/broker restart, database writes or command registration occurred. The local runtime remains on earlier loaded code. Choosing original mainframe data versus the current local bot database and approving production activation remain owner decisions. This assessment and handoff addition were written after publication and are not included in `bcde78a`.

## Integration branch rename

At the owner's request, birthday recovery was signed as `3bfb0dd` and fast-forwarded from `features/birthday-recovery` into local `caitlyn-2.0`. The integration branch was then renamed to `caitlyn-3.0` both locally and on GitHub. GitHub's branch still points to `c468729`; renaming it did not publish the newer local commits. Future feature branches start from `caitlyn-3.0`, and the old names in historical plans/checkpoints describe their original context. `main`, package versions, deployment configuration, and the homeserver are unchanged.

## Logging release scope and verification (2026-09-18)

The owner requested committing the logging work, merging into `caitlyn-3.0`, and pushing both the feature and integration branches before reviewing mainframe deployment. The release includes connection/log-delivery/birthday recovery, plain wording and metadata-only collection logs. Parked Reddit implementation/tests and their separate documentation changes are excluded and preserved in the working tree. `main` and the historical unsafe backup/feature refs must not be published by this operation.

The exact staged source snapshot, without parked Reddit files, passed `npm run check`: **392 tests passed, one optional PostgreSQL skip (393 total)**, plus typecheck, lint and clean build. The full development checkout previously passed 433 tests plus one skip; the difference is the intentionally excluded Reddit suite. A scan of all 509 staged tracked files found no exact, URL-encoded or base64 matches for locally configured secrets. No dependencies were installed; the staged test copy reused existing `node_modules`. Whitespace checks passed. No bot/broker restart or command registration is required for Git publication; runtime activation is still a separate step.

## Previous continuation: collection and feature logs (2026-09-18)

The owner requested logs across existing data collection/features and explicitly selected **details only, no message text**. Added a shared selected-field formatter (`core/dataLog.ts`) with bounded scalar values, escaped control characters and server context. DEBUG covers received messages, committed message counts/daily activity, voice joins/moves/leaves and seconds counted, duplicate/missing sessions, commands, read-only profile/statistics lookups, AI processing/memory and social request/content summaries. INFO records committed birthday/settings changes. Logs distinguish receipt, collection, reads, saves and sends; activity save logs occur after transaction commit, and source message/channel IDs correlate message receipt with its count update. See [the full coverage and privacy guide](data-logging.md).

Removed older AI text snippets. AI service/memory/reply error paths now expose only safe known network/HTTP reasons because raw exceptions can contain request bodies. No message text, birth-date values, vectors, attachments, source URLs, provider payloads or credentials are added to the new collection logs. Known bot events avoid log feedback, and no-op social cancellations stay quiet. Existing private main-server routing, filter choices, bounded queue, collection scope, disabled-by-default AI and parked Reddit work are preserved. There are no new database fields, tracking permissions, commands, archival files or live service calls for diagnostics.

`npm run check` passed **433 tests, one optional PostgreSQL skip (434 total)**, including typecheck, lint and clean build; `git diff --check` passed. Ten added tests plus extended social assertions cover metadata/privacy, concurrent server scope, post-commit success only, failed commits, voice duplicates/durations, birthday subject/requester and omitted dates, bot-event suppression, affected cancellation counts, AI reply failures and memory content/vector/error redaction. Tests use fake services and the existing synthetic embedding data URL.

**Nothing installed or upgraded.** No `.env`, database, log-level selection, bot/broker/image, homeserver, command registration, commit, merge or push change. The local bot is still on the previous runtime code. A new activation question asks whether to restart only the local bot and confirm the homeserver is inactive; do not assume approval. Prior runtime details below remain the restart reference, but verify PIDs/cwd first. All logging changes remain uncommitted above `f28a141` alongside separately parked Reddit edits.

## Previous continuation: plain-language logs (2026-09-18)

The owner asked for simple words in logs, specifically replacing “reconciliation”. Updated birthday and social-message checks to explain whether a message was found in Discord and why no second copy will be sent. Connection messages explain interruptions, reconnect attempts and when help is needed; known network codes retain their useful code alongside a plain explanation. Private log-delivery notices distinguish parts that were dropped, could not be sent, or may already have been sent. Birthday scheduling and conflicting voice-session errors also use simpler wording. Internal database/status names, safety checks, retry limits, log levels and privacy rules are unchanged. Useful technical details and third-party error text remain available for troubleshooting. The wording convention is recorded in [private logging documentation](discord-logging.md#log-wording).

`npm run check` passed **423 tests, one optional PostgreSQL skip (424 total)**, including typecheck, lint and clean build; whitespace checks passed. Updated wording assertions and added coverage for all seven known network-error explanations without leaking raw diagnostics or downgrading unknown errors. No packages/tools installed, bot/broker restart, live Discord messages, registrations, database/configuration changes, commit or push. These wording changes and the prior recovery fixes remain uncommitted and need a local bot restart to become active; the pending restart choice is still unanswered. Broader new logging remains a separate follow-up. Parked Reddit work is preserved.

## Previous continuation: graceful gateway/logging/scheduler recovery (2026-09-18)

The owner approved fixes after reviewing September 15/16 gateway handshake errors and inaccurate log-loss warnings, with broader logging additions requested afterward. Both supplied UTC error times match macOS background wakes exactly after conversion to Amsterdam time (September 15 22:12:08; September 16 11:33:20), strongly implicating sleep/network availability. A local synthetic reproduction queued six log chunks, failed one batch and confirmed the old code reported only one omitted chunk. The September 18 running-bot audit found one missed birthday tick during sleep, subsequent healthy checks, a successful Instagram delivery/source removal, healthy PostgreSQL/worker and no outstanding social jobs. Retained Discord startup logs place bot readiness at **18:41:04 UTC** (the earlier 16:41 label below is inaccurate).

Implemented grouped transient gateway warnings, persistent/fatal escalation and recovery durations without adding a second reconnect loop; actual Discord readiness within the existing login deadline; bounded retained log batches with 30/60/120/240-second backoff, five-attempt/15-minute limits, stable nonce and destination/privacy fencing; exact overflow/failed/unconfirmed chunk counts; observation of late send acknowledgements without blind uncertain retries; and shared scheduler logging plus one coalesced current-day check after missed ticks/readiness recovery. Shutdown clears owned timers/listeners. Log storage remains bounded memory/best-effort, not a durable archive. See [private logging behavior](discord-logging.md).

`npm run check` passed **422 tests, one optional PostgreSQL skip (423 total)**, including typecheck, lint and clean build. Eighteen new regression cases cover gateway/readiness, real transport classification, six-chunk failures, safe retries, expiry, late outcomes, overflow, configuration races and missed scheduler recovery. Tests use fake services; no diagnostic Discord messages, outage injection, migrations or registrations were performed. Nothing installed/upgraded; existing dependencies reused. `.env`, homeserver, worker image/backend and parked Reddit work are unchanged.

No restart yet: bot **318** / session **98628** and broker **97148** / session **5329** were left running on the prior code. The owner has been asked whether to activate now or after the next logging additions. Preserve the existing socket `/tmp/caitlyn-instagram-runtime-kV7aHH/worker.sock`, local DB overrides, social-on/AI-off settings, and verify process/cwd plus homeserver inactivity before a later restart. No slash-command definitions changed. These fixes are uncommitted above the signed Instagram commit `f28a141`, which was fast-forwarded locally into `caitlyn-3.0`; neither branch was pushed.

## Instagram integration scope and verification (2026-09-18)

The owner requested an Instagram commit and merge into `caitlyn-3.0`. This checkpoint includes only Instagram integration, access notices, tests and documentation; the parked Reddit parser/rendering/media work remains uncommitted in the active checkout. The exact staged snapshot passed `npm run check` separately from those Reddit files: **363 tests passed, one optional PostgreSQL skip (364 total)**, plus typecheck, lint and clean build. Staged whitespace and local secret-value checks passed without displaying credentials. Both GitHub branch tips were verified through the existing GitHub login at `a82cfc0`; SSH fetch was unavailable. No push was requested, and the running local bot/broker and `.env` were left unchanged.

Instagram carousels accept up to eight images, videos or mixed items within the existing byte budgets. Automated ordered-media checks pass; real mixed-carousel extraction and Discord presentation still need an owner-supplied example.

## Latest continuation: public Instagram fixed, access notices added, local testing active (2026-09-18)

The owner confirmed both Instagram samples are public and asked for nicely formatted private/login failure messages. Root causes: HTML was a client-rendered shell requiring the pinned extractor's logged-out same-origin GraphQL hydration request, and image candidates contained URLs without dimensions. Added one bounded public hydration request (no account, impersonation, alternate endpoint or denial retries), and retained URL-only image candidates for actual decoder validation. Metadata absence and normal login links no longer imply private access.

Owner photo `DdUCjIygdfq` now passes real isolated delivery: 2,438,382 bytes, one verified image, complete caption/card. Reel `DdGpKGeo7Lc` passes with audio: 5,014,030 bytes, one verified MP4; at a diagnostic 1 MiB cap it compresses to 905,235 bytes with audio and no omissions. No diagnostic sent Discord messages. Explicit private/login-required/401 results generate an attributed lock-icon reply with a compact source link, no ping/comment duplication, and original preservation. Durable claim/nonce/reconciliation prevent duplicate notices; they never authorize original deletion. Generic 403, rate limits, challenges, redirects, empty metadata and unavailable posts are not labelled private.

`npm run check` passed **404 tests, one optional PostgreSQL skip (405 total)**; **10 Python fixtures** separately passed in a network-none container. All owned test containers/volumes/media/staging sockets were cleaned up. No packages/tools installed/upgraded. Two source-only image revisions were built with all seven dependency layers unchanged: intermediate `sha256:402f2f9903e7d765623717df407116dd6f4e194fea8203352ec6e4bbeaf3791b` and selected `sha256:50894c2e409bfcb61d36274cdfa66f5701b965cc38e87c0095de0b4ee18efa4d` (`caitlyn-media-instagram:local`). The earlier shared-compression tag/image remains unchanged.

The owner explicitly approved local restart/registration and confirmed the homeserver is inactive. Old bot/broker PIDs were already gone, so no unrelated process was stopped. The prior host FxEmbed API-key file was missing; restored only the existing local API key privately from the running container, validated authenticated health, and saved it to the new 0700 runtime directory/0600 file. No account/container configuration or browser session changed.

- Broker **97148**, session **5329**, ready **16:40:45 UTC**: context `colima-caitlyn-media-test`, image `caitlyn-media-instagram:local`, provider fxembed, FxEmbed `http://127.0.0.1:8787`, key `/tmp/caitlyn-instagram-runtime-kV7aHH/fxembed.key`, socket `/tmp/caitlyn-instagram-runtime-kV7aHH/worker.sock`, `LOG_LEVEL=INFO`. Health: 200/idle/platforms x,tiktok,instagram.
- Bot **318**, session **98628**, online **16:41:04 UTC**: same socket, `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test SOCIAL_MEDIA_ENABLED=true LLM_ENABLED=false LOG_LEVEL=INFO`. Startup confirmed PostgreSQL, 14 commands/8 events, social/private logging configured and AI off. Exactly one local bot and broker confirmed; reverify PID/cwd before stopping.
- `/social` alone was patched/read back in the configured guild at **16:40:48 UTC**. Other commands and global registration unchanged. No `.env`, database schema/config file, homeserver, commit, merge or push change. Runtime overrides are process-only. Old stale sockets were left untouched.

Next: owner Discord playback/layout/source-cleanup acceptance, a real mixed-carousel example, and actual private/login-card acceptance. Reddit remains parked with all prior offline work preserved. See [Instagram](instagram.md) for current limits and repeatable checks; older unavailable/disabled entries below are historical.

## Previous continuation: shared compression publication/integration checkpoint (2026-09-14)

The owner requested committing and pushing both compression continuations, then merging into `caitlyn-3.0`. This checkpoint includes the shared X/TikTok pipeline, tests, documentation and future Reddit integration contract described below. The final `npm run check` passed **345 tests, one optional PostgreSQL skip (346 total)**; typecheck/lint/clean build and whitespace checks passed. The 493-file exact/URL-encoded/base64 scan against locally configured secrets found no matches. No installation or runtime restart is needed for publication; the active processes/images remain as recorded in the preceding implementation checkpoint below. Earlier descriptions of this work as uncommitted are historical.

Before integration, GitHub's feature branch was verified at `870dfbbcbb4d3f6390ebdd27033c2d9f55a78826`, and GitHub's `caitlyn-3.0` at `c468729b4c3110f21a3967272d45d4f03a30ae44`. Local `caitlyn-3.0` still referenced original `227ce5e`; its sanitized equivalent `0e77723` is already an ancestor of the current feature branch. Verified that the only tree difference is the configured password replaced with the documented redaction marker in `docs/handoff.md`; no code or unrelated local changes are lost by reconciling that reference. Existing local-only rollback ref `refs/backup/caitlyn-social-media-20260913-pre-redaction` retains the original history and must never be pushed. Integration uses the clean history, and both GitHub updates can fast-forward without a force-push or unsafe merge parent.

Resolve the signed `feat: share bounded video compression across X and TikTok` checkpoint from Git and verify both remote branch SHAs when continuing; do not infer publication from this pre-publication note alone. Do not publish other old local feature refs. `main`, homeserver configuration and credentials are outside this integration.

## Previous continuation: shared X/TikTok video compression, locally active (2026-09-14)

The owner requested X compression too, with the same path available for Reddit. Added the platform-independent `scripts/mediaSandbox/videoDelivery.ts`: original verification and the size-only fallback now live together, with approved candidate selection/downloading injected by each adapter. TikTok no longer depends on the X worker for verification. X applies the shared fallback independently to parent and quoted videos, preserving media ownership/order and using the remaining aggregate byte budget. The X downloader permits up to 48 MiB only for temporary video inputs, validates that limit before connecting, and retains declared/streamed bounds. Images and silent animated GIFs remain original-only. Strict wire validation permits boolean compression markers only on video attachments, and each affected card shows one reduced-quality notice. Existing private guild logging and complete-delivery/source-retention rules remain in force.

**Reddit extraction is not enabled yet.** Its future adapter should provide an approved compatible MP4/audio source to this same helper; it still needs its own extraction, egress/access policy, normalization and transport validation. Do not interpret the shared helper as completed Reddit support. No source/worker/output/deadline limits were increased beyond the already approved TikTok compression policy; multiple X videos share the existing whole-job 90-second worker deadline and gateway traffic cap.

Verification:

- `npm run check`: **345 passed, one optional PostgreSQL skip (346 total)**, typecheck/lint/clean build successful. Eight new tests cover parent+quote compression, mixed original/compressed ownership, multi-video order/single notices, failure/source-retention behavior, four-download/source caps, silent GIFs, aggregate budget exhaustion, strict marker/byte validation, and content-free guild-scoped logging.
- Offline existing-container Python tests: **7 X + 13 TikTok passed**. X tests include video-only 48 MiB cap, invalid settings rejected before network access, declared bounds and streamed overflow. No new dependencies installed.
- Real X example at a **diagnostic-only 2 MiB cap**: ready, **1,845,374-byte compressed MP4**, full duration/audio validated, no omissions, completed **00:18:22 UTC**. The 141.109-second clip intentionally failed the minimum-quality floor at 256/512 KiB; those were safe partial results, not successful delivery. Production still uses 8 MiB/file.
- Real X quote at the normal cap: **two original MP4s / two cards / 1,348,582 bytes / zero omissions**, **00:18:24 UTC**. Both parent-and-quote compression is covered by synthetic integration tests; this particular real quote did not need compression.
- Shared-image regressions: long TikTok **7,388,255 bytes compressed** at **00:19:39 UTC**; ordinary X **3,162,626 bytes uncompressed** at **00:19:40 UTC**; short TikTok **2,953,029 bytes uncompressed** at **00:19:42 UTC**. All ready, full attachment counts and no omissions. No diagnostic Discord message was sent/deleted and no historical job replayed.
- `git diff --check` passed. Exact/URL-encoded/base64 scan of **493 working-tree files** against locally configured secrets returned zero findings.

Local activation: verified the command/cwd of bot **33927** and broker **31613**, checked broker idle/no leftover media resources, then stopped both gracefully at **00:15:31 UTC** before real candidate probes. Both old sessions exited successfully and their socket was removed by normal shutdown. Restarted on the existing private socket directory after all probes cleaned up:

- Broker **19674**, session **6362**, ready **2026-09-14 00:20:13 UTC**. Uses `SOCIAL_WORKER_IMAGE=caitlyn-media-shared-compression:local`; other settings unchanged: Docker context `colima-caitlyn-media-test`, FxEmbed loopback `http://127.0.0.1:8787`, private key file `/tmp/caitlyn-owned-api.lThfz1/fxembed.key`, socket `/tmp/caitlyn-compression-runtime.WBLLhl/worker.sock`, provider `fxembed`, `LOG_LEVEL=INFO`. Health returned **200 / idle / platforms=[x,tiktok]**.
- Bot **22790**, session **62973**, started **00:20:28 UTC**, online **00:20:34 UTC**, using the same local `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test`, socket, `SOCIAL_MEDIA_ENABLED=true LLM_ENABLED=false LOG_LEVEL=INFO`. Startup confirmed PostgreSQL, 14 commands/8 events, configured social/private logging and AI off. Exactly one bot and one broker are running in this checkout; verify identity/cwd before future stops.
- Selected source-only image **`sha256:cb53037e23890b1801fc2bc16da30780f9892866da3e388b98fa77deb55b44c2`**. All seven dependency/base layers reused offline; prior compression/TikTok/API images retained. FxEmbed account container/session, credentials, `.env`, database, channel settings and homeserver are untouched; owner previously confirmed the homeserver inactive.

**Nothing installed**, no new slash commands/registration changes, migrations, autostart, commit, push or merge. Both compression continuations remain **uncommitted** above the already published `870dfbb`. Documentation is under `docs/` with file headers/folders preserved. Next manual check: a fresh oversized X video/quoted-video link in an enabled Discord channel, then review quality/playback/source cleanup. See [shared compression](video-compression.md) for the policy and future Reddit integration contract; preserve the older-history publication warnings below.

## Previous continuation: bounded TikTok compression, locally active (2026-09-14)

Completed the approved checkpoint first: signed commit **`870dfbbcbb4d3f6390ebdd27033c2d9f55a78826`**, `feat: add isolated TikTok previews and content-aware social cards`, was pushed normally to GitHub's `codex/social-media-replacement`. The remote SHA was verified exactly, and its local tracking ref refreshed. 1Password signing succeeded after the owner unlocked it; HTTPS with the existing GitHub CLI credential helper handled publication. No merge, force-push or unsafe rollback/older ancestry was published.

Implemented the next feature as a **TikTok-only, size-failure compression fallback**, documented in [video compression](video-compression.md). Fitting originals remain preferred. At most three direct candidates plus one smallest eligible source retain the four-download limit; temporary sources are capped at 48 MiB, delivered files at 8 MiB. The existing isolated worker performs two bounded single-threaded H.264/AAC passes, verifies actual output bytes, checks complete audio/video durations and fully decodes output before attachment. Truncated/invalid/timed-out output remains partial and preserves the source. Verified compression adds an explicit reduced-quality card notice and content-free private guild logging. X handling, permissions, source deletion/identity fences, credentials and database schema are unchanged. The new helper lives under `scripts/mediaSandbox/` and is copied into the source-only worker image; operational scripts are intentionally excluded from the production bot's `dist` build.

Verification:

- `npm run check`: **337 passed, one optional PostgreSQL skip (338 total)**; typecheck, lint, clean build and whitespace checks pass. Eight new tests cover planning, fixed local process arguments, failure/timeout/completeness handling, candidate/download caps and hostile compression markers. No new live PostgreSQL test was needed for this media-only change.
- Existing-container Python request suite: **13 passed** with no networking, including the 48 MiB boundary. No-network synthetic clip: **15,296,653 bytes → 457,485 bytes** under a 512 KiB budget, with full audio/video verification.
- Final-image real owner `nicoiscold` example: **ready, 7,388,255 bytes, one compressed MP4, one embed, zero omitted media**, completed at **2026-09-13 23:52:49 UTC** in about 35 seconds. All 654 seconds and audio passed the output checks. This long clip falls to a 240-pixel longest side/15 fps; quality is deliberately reduced, not equivalent to the original. An initial slower-preset candidate failed safely; `ultrafast` and frame-rate reduction before scaling passed without increasing existing CPU/deadline budgets.
- Regressions: X video **3,162,626 bytes** and small TikTok **2,953,029 bytes**, both complete and uncompressed at **23:53:59/23:54:01 UTC**. Diagnostic probes sent/deleted no Discord messages and cleaned their own containers/media/IPC/socket directories. Manual Discord desktop/mobile playback and fresh share links still need owner testing.

The previous local bot/broker were already stopped. The owner explicitly approved **starting locally** and confirmed the **homeserver is inactive**. Verified no matching local process or disposable worker resources before activation. Current local state (verify command/cwd and health again before any future stop):

- Broker **31613**, session **39171**, ready **2026-09-13 23:58:22 UTC**, cwd this checkout. Settings: `SOCIAL_WORKER_DOCKER_CONTEXT=colima-caitlyn-media-test SOCIAL_WORKER_IMAGE=caitlyn-media-compression:local SOCIAL_X_PROVIDER=fxembed SOCIAL_FXEMBED_URL=http://127.0.0.1:8787 SOCIAL_FXEMBED_KEY_FILE=/tmp/caitlyn-owned-api.lThfz1/fxembed.key SOCIAL_WORKER_SOCKET=/tmp/caitlyn-compression-runtime.WBLLhl/worker.sock LOG_LEVEL=INFO`.
- Bot **33927**, session **12989**, started **23:58:33 UTC**, online **23:58:34 UTC**. Settings: `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test SOCIAL_MEDIA_ENABLED=true SOCIAL_WORKER_SOCKET=/tmp/caitlyn-compression-runtime.WBLLhl/worker.sock LLM_ENABLED=false LOG_LEVEL=INFO`. Startup confirmed PostgreSQL connection, 14 commands, 8 events, social/private logging configured and AI disabled. Exactly one bot and one broker are running in this checkout.
- Fresh socket directory is owner-only; the existing private API key file and FxEmbed account container/session are unchanged. Broker health returned **200 / idle / platforms=[x,tiktok]** before bot activation. Old socket paths are not current and were not blindly unlinked.
- Selected image **`caitlyn-media-compression:local` = `sha256:04cb972158c15518348ef22d8f46e4b518b229832d932586b1b5fd6762c3eeb5`**. All seven dependency/base layers were reused offline. Prior `caitlyn-media-tiktok:local` remains at `sha256:0246502369c30a1536d64f99aed4b834ad37e59e551d1b9c3553f6915b49bd8c`; no images pruned.

**Nothing installed.** No new commands/registration changes, migrations, `.env` edits, autostart or homeserver changes. The checkpoint above is published; **the new compression implementation remains uncommitted** on the same sanitized feature branch. Next: send a fresh long TikTok in an already enabled channel to assess playback/quality. Photo slideshows/soundtracks, Reddit, broader X compression and homeserver deployment remain separate work. Preserve the historical publication warnings below.

## Previous continuation: approved TikTok/presentation checkpoint (2026-09-14)

The owner approved the proposed next step: commit/push the current TikTok and presentation work, then begin larger-video handling. The checkpoint quality gate passed **329 tests, one optional PostgreSQL skip (330 total)**, with typecheck/lint/build and whitespace checks passing. A 488-file exact/URL-encoded/base64 scan against locally configured secrets returned no findings. The remote feature branch was verified at the sanitized base `58aacdd`; only `codex/social-media-replacement` is being published, without a merge, force-push or unsafe older ancestry. Resolve the signed checkpoint hash from Git when continuing; confirm the remote result before claiming publication.

Fresh TikTok mobile-share playback remains an explicitly unverified manual check, not a reason to invent a successful test or replay historical jobs. Existing local bot/broker configuration from the preceding section is unchanged for this checkpoint. Nothing installed. The planned next implementation is bounded larger-video handling inside the isolated worker, retaining original messages on failure and leaving photo slideshows, Reddit and homeserver deployment for later.

## Previous continuation: Twitter-style quote ordering (2026-09-13)

The owner requested the quoting tweet at the top and the quoted embed underneath its content. Inspection confirmed normalization already distinguishes/orders these authors correctly. The visible break was the sharing/completion metadata between the two cards. Quote previews now keep the quoting author's text/media first, followed by the quoted author's content in a muted card without a large title. A compact `Quoted post` source label preserves context. `Shared by` is below the quoted text/source; the renderer returns `footerEmbedIndex` so runtime places the exact `Caitlyn preview` marker on that lower content card instead of interrupting the pair. Gallery continuation numbers/authors are untouched. Unavailable quotes and ordinary X/TikTok posts keep their main-card completion behavior.

Classic embeds remain separate stacked cards, not a pixel-identical nested Twitter widget. Native video placement is still controlled by Discord. No extraction, media ordering/ownership, source-retention rules, IDs, credentials, database schema or identity requirements changed. Existing reconciliation already checks every embed and requires saved-message identity or the matching nonce; no broadening was needed. Old messages are not rewritten.

Verification: `npm run check` **329 passed, one optional PostgreSQL skip (330 total)**, plus typecheck/lint/build and `git diff --check`. Three new tests cover parent/quote ordering across empty/single/four-image layouts, lower-card footer placement in runtime, and nonce/saved-identity recovery and cleanup with the footer on the quoted card. Existing aggregate limits, missing quotes, independent media ownership and single attribution tests pass.

Local activation: verified bot **71500** and broker **7141** command/cwd and idle health; gracefully stopped bot 71500 and confirmed exit. Replacement bot **72078**, session **24376**, started **20:42:41 UTC**, with unchanged local database/private socket/social-on/AI-off settings. Broker, account backend and images are unchanged. Test with a fresh owner-sent quote link; no diagnostic Discord messages or historical replays were sent. **Nothing installed.** No registrations, migrations, environment/homeserver changes, commits, pushes or merges. Work remains uncommitted on the sanitized feature branch.

## Previous continuation: sender attribution inside the embed (2026-09-13)

Moved `Shared by @sender` into the main card's description below its caption/source line, at the owner's request. Runtime passes the validated source job's Discord user ID to the renderer; malformed IDs are rejected. Attribution appears once per preview, never on quoted-author or gallery continuation cards. Sender commentary stays above the card; link-only shares now have no standalone message content. Quote authors, native media attachments, the exact `Caitlyn preview` footer, safe mentions, logging and nonce-based identity are unchanged.

The original commentary-length guard still includes its legacy attribution overhead. Moving the visible attribution must not expand deletion eligibility for recovered older previews which omitted long commentary. Over-budget commentary still produces a retention notice and preserves the source. Existing preview messages were not edited/replayed.

`npm run check`: **326 passed, one optional PostgreSQL skip (327 total)**; typecheck, lint, build and `git diff --check` passed. Three additional tests cover all supported layouts with exactly one in-card sender, malformed sender input, and the legacy commentary/deletion boundary. Existing tests verify runtime wiring, no standalone attribution, unchanged footer identity, and aggregate embed limits including the new attribution line.

Local activation: verified bot **79786** and broker **7141** command/cwd, checked broker idle, then gracefully stopped bot 79786 and confirmed exit. Replacement bot **71500**, session **15650**, started **20:35:04 UTC**, with the same local database/private socket/social-on/AI-off settings below. Broker/account/backend/images are unchanged. Fresh owner-sent links are needed to check this latest placement. **Nothing installed**; no environment changes, registrations, migrations, homeserver changes, commits, pushes or merges. All current feature/presentation changes remain uncommitted on the sanitized feature branch.

## Previous continuation: caption-first, content-aware previews (2026-09-13)

The owner requested a minimal original link, visible post captions, and layouts appropriate to image/video/text/quote content. `core/socialPostRender.ts` now omits the large main-card link title and puts the extracted caption first, followed by a compact platform/media label and `Original ↗` link. Authors remain clickable. Sender commentary/mentions stay separate above the card. Media-only and captionless quote posts have no empty-caption filler; unsupported/unrelated URLs in real commentary are not stripped indiscriminately.

Images use the main card plus numbered, separately attributed continuation images. Videos/GIFs keep verified native MP4 attachments and readable message-scoped filenames such as `tiktok-post-video-1.mp4`; no post/job IDs are added to display names. Text-only posts get a 3,600-character reading budget; media captions and quote pairs use 1,800 characters per card. Full extracted text remains attached when needed and within budget. Quoted posts have a distinct-color card, their own author/caption/source, and independently owned media. Missing media/text/quotes remain explicit and incomplete; no deletion or recovery guarantees were weakened. The first footer remains exactly `Caitlyn preview` for nonce/saved-message identity checks.

Runtime DEBUG diagnostics report only embed/file counts and completeness through existing guild-scoped logging. Render-only incompleteness also produces the partial-preview warning. Private log routing, per-channel opt-ins, extraction/account/worker configuration, and source-cleanup state transitions are unchanged. See [presentation behavior](social-delivery.md#behavior-and-configuration) and [TikTok](tiktok.md).

Verification: `npm run check` passed **323 tests, one optional PostgreSQL skip (324 total)**, with TypeScript/lint/production build passing. Seven additional tests cover content-type labels, text-vs-caption budgets and Unicode boundaries, distinct/captionless/unavailable quotes, gallery omissions/numbering, aggregate Discord limits, and content-free render logs/completeness fencing. Updated existing X/TikTok delivery tests check compact source links, filenames and captions. `git diff --check` passed. No new extraction probe, database mutation, Discord test message, or historical preview edit/replay was performed.

Live local activation: checked bot **7370** and broker **7141** command/cwd; broker health was idle with X/TikTok enabled. Bot 7370 stopped gracefully and exited before replacement bot **79786**, session **94319**, started at **20:20:31 UTC** with the same local database/socket/social-on/AI-off settings recorded below. Broker 7141 and the worker/FxEmbed images/account session were not restarted or changed. Prior bot logs confirmed successful owner-triggered TikTok delivery/source cleanup at **20:05 UTC** and X delivery/source cleanup at **20:08 UTC**; those precede this layout change and do not verify its visual appearance or mobile playback. Send fresh links to check the new layout; existing previews are not retroactively edited.

**Nothing installed.** No command JSON changed, so the existing 14 commands need no re-registration. No migrations, homeserver changes, commit, push, or merge. This presentation update and the preceding TikTok feature remain uncommitted on the sanitized feature branch; preserve the older-history publication warnings below.

## Previous continuation: TikTok video previews, locally active (2026-09-13)

The owner selected TikTok as the next feature after publishing signed/sanitized `58aacdd`. Implemented public video and mobile-share delivery using the existing pinned extractor inside the isolated media worker, not a hosted fixer or the X account. Added `core/socialTikTokPost.ts`, `scripts/mediaSandbox/tikTokPostWorker.ts`, and `tikTokMedia.py`; extended the strict Unix delivery route/decoder, platform-aware renderer, source-caption handling, namespaced queue identities, and existing `/social` command copy. No migration is needed. File headers/folder structure are preserved; [TikTok documentation](tiktok.md) is under `docs/`, README remains at the root.

The same configured channels now handle X and TikTok, retaining sender-only mentions, clean `Caitlyn preview` footers, typing feedback, bounded retries and safe source cleanup. Unsupported/partial/ambiguous outcomes preserve the original; standalone photo/live/unsupported links are untouched. TikTok aliases are resolved only in the sandbox; leased database transactions can reuse a confirmed complete same-source preview without duplicate sends. Original opaque keys remain stable for all-sibling cleanup and X numeric keys remain backward compatible. An incomplete or ambiguous sibling never authorizes deletion or a second send. Private guild logging remains unchanged and reports the fixed `mode=tiktok` alongside content-free outcomes/timings.

CDN verification initially rejected TikTok's `v16/v19-webapp-prime.tiktok.com` hosts; their bounded hostname pattern is now recognized. A real media 403 was resolved by preserving only the current job's anonymous request user agent, canonical referer and domain-scoped anonymous cookies in private 0600 files inside worker tmpfs. No host/browser account, `.env` secret or X session enters this path. Media URLs go over subprocess stdin; context/headers/cookies never leave the worker. Both production gateways now restrict egress to their selected platform in addition to existing public-DNS/private-network/traffic limits. Missing/malformed X backend configuration logs a warning without disabling independent TikTok jobs.

Verification:

- `npm run check`: **316 passed, one optional PostgreSQL skip (317 total)**, including new TikTok normalization/transport/worker tests, mixed-source rendering and admission, duplicate resolution, partial/blocked behavior, compiled output, and Unicode boundaries.
- Real disposable PostgreSQL suite: **20 passed**, including mixed X/TikTok numeric identities, stable share keys, confirmed alias reuse, uncertain siblings and source deletion fencing. Only a newly created synthetic test database was used and removed; `caitlyn_test` data/schema are unchanged.
- Python request/context suite: **12 passed** in the existing network-none image with injected requests, covering redirects, private hosts, MIME/streaming caps, no retries on access denial, private anonymous context and no context fields in output.
- TikTok official `scout2015` example: complete **2,953,029-byte H.264/AAC MP4**, decode and renderer passed; final image recheck at **19:51:19 UTC**. The owner's 654-second `nicoiscold` example correctly returned partial `size_limit`; compatible metadata reports approximately 37 MB, beyond the 8 MiB attachment cap.
- X regression: existing `iClipCx` example still returned ready, **3,162,626 bytes**, one verified MP4/one embed, no omissions. An older upstream TikTok example failed extraction. Old mobile-share samples did not resolve to a post; the inspected `/t/` token redirected to TikTok's homepage, now classified unsupported rather than implying an account gate. Fresh mobile links and actual Discord desktop/mobile playback/source cleanup remain owner testing.

Live local activation (verify identities/cwd again before future stops):

- Gracefully stopped prior bot **28222** and broker **94635** only after checking their command/cwd and broker idle/no leftover worker resources.
- Broker **7141**, session **78641**, started **19:52:14 UTC**: `SOCIAL_WORKER_DOCKER_CONTEXT=colima-caitlyn-media-test SOCIAL_WORKER_IMAGE=caitlyn-media-tiktok:local SOCIAL_X_PROVIDER=fxembed SOCIAL_FXEMBED_URL=http://127.0.0.1:8787 SOCIAL_FXEMBED_KEY_FILE=/tmp/caitlyn-owned-api.lThfz1/fxembed.key SOCIAL_WORKER_SOCKET=/tmp/caitlyn-owned-api.lThfz1/worker.sock LOG_LEVEL=INFO`. Health reports idle and `platforms=[x,tiktok]`.
- Bot **7370**, session **8287**, started **19:52:15 UTC**, online **19:52:16 UTC**, same local `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test SOCIAL_MEDIA_ENABLED=true SOCIAL_WORKER_SOCKET=/tmp/caitlyn-owned-api.lThfz1/worker.sock LLM_ENABLED=false LOG_LEVEL=INFO`. AI remains off; one bot and one broker are running in this checkout.
- Selected worker image `caitlyn-media-tiktok:local` = **sha256:0246502369c30a1536d64f99aed4b834ad37e59e551d1b9c3553f6915b49bd8c**. Source-only offline builds reused all **seven** baseline dependency/base layers. Original `caitlyn-media-api:local` remains unchanged at `sha256:e63b987a52171befed7615eff7cfbe41bb5d01e27bcc4155f957a336399b0870`; no images were pruned.
- Verified the bot identity and configured **MandemHQ** guild through Discord REST, then updated and re-read only existing `/social` command **1548315849499344901**. Description is `Configure X and TikTok previews in this server`. All 14 command names remain; no other guild/global command registrations changed.
- FxEmbed account container/session, local private key/socket directory, PostgreSQL configuration, existing channel opt-ins and main-server logging configuration are unchanged. The homeserver remains untouched/inactive. No test Discord message was sent or deleted by the diagnostics, and no historical job was replayed.

**Nothing installed** on the Mac or in dependency layers. Reused Node/TypeScript, the pinned yt-dlp and existing Python/FFmpeg tools; temporary build/test containers, media, IPC volumes and synthetic database were removed. This feature is **uncommitted** on `codex/social-media-replacement`; no new commit, push or merge was requested/performed. Keep the previous history-cleanup rules below: never publish unsafe rollback/older local ancestry. Next: a fresh owner-sent short TikTok video/mobile-share link in the already opted-in Discord channel. TikTok photo slideshows/soundtracks, larger-video processing, private/account-only access and homeserver supervision are separate work.

## Previous continuation: clean preview footer and publication checkpoint (2026-09-13)

At the owner's request, new preview footers now say only `Caitlyn preview`, without the internal job UUID. Reconciliation/removal/source cleanup still require this bot's identity and any saved Discord message ID; new clean-footer uncertain sends additionally need the matching Discord nonce when no saved message ID exists. Older UUID footers remain supported. If Discord omits nonce on an ambiguous send, the job stays uncertain for operator review: shared captions/URLs/footers never authorize a resend or source deletion. No invisible identifiers were added to text or source links, and no historical Discord messages were edited.

`npm run check` passes **300 tests, one optional PostgreSQL skip (301 total)**. Three new regression tests cover clean-footer saved identity, nonce-based uncertain recovery/legacy compatibility, and rejection of wrong authors/webhooks/references/message IDs. Existing runtime tests verify the job ID is absent from embed text.

The owner requested a commit and push of the accumulated work on `codex/social-media-replacement` (not a merge). A pre-publication credential check found a database password in an older handoff note and seven unpublished ancestor commits after remote integration base `c468729`. The owner explicitly approved sanitizing that unpublished history, retaining a local rollback reference, and using a normal push without rewriting existing remote branches. With 1Password unlocked, all eight unpublished commits were rebuilt and signed; the seven contaminated handoff blobs were redacted without displaying the value. Author/committer identities, dates, messages, and all other files were preserved. The final tracked tree at `2d26b3f` is identical to the original already-redacted checkpoint `89129e8`.

The cleaned outgoing history passed an exact/URL-encoded/base64 check against the configured local secrets across **597 commit/blob objects**, with no matches. The following map identifies the rewritten checkpoints; older sections retain their original historical hashes:

| Original local commit | Sanitized signed commit | Checkpoint |
| --- | --- | --- |
| `8ea6f5e` | `09ac21d` | Modernization |
| `b998543` | `3244bbb` | Resilience |
| `7ad30ed` | `ed3c2ea` | Private logging |
| `3bfb0dd` | `84c16bd` | Birthday recovery |
| `227ce5e` | `0e77723` | Branch rename documentation |
| `03eab46` | `7b83163` | Isolated social-media feasibility |
| `0f3dee4` | `50703ee` | X extraction and rendering |
| `89129e8` | `2d26b3f` | Self-hosted X, feature gates, feedback, clean footer |

Local-only rollback ref `refs/backup/caitlyn-social-media-20260913-pre-redaction` retains the original `89129e8` ancestry. Older local integration/feature refs were not rewritten and can still retain the private value: **never publish that rollback ref or the old ancestry, and do not merge the old ancestry into the cleaned feature branch**. A future integration must reconcile those local refs onto the sanitized history first. Existing GitHub branches are unchanged; `caitlyn-3.0` remains at `c468729`. This checkpoint is prepared for an explicit normal push of only `codex/social-media-replacement`; verify its remote SHA against local `HEAD` after publication. No force-push, merge, credential rotation or homeserver change is authorized by this cleanup.

The local bot was gracefully restarted from verified PID **50640** to **28222**, session **38838**, at **18:15:08 UTC**, with the same local database/socket/social-enabled/AI-disabled settings from the preceding checkpoint. Broker **94635** and the account backend remain unchanged. New posts receive the clean footer; old posts are not bulk-edited. Nothing installed and no command registrations, database migrations, homeserver changes or merge were performed for this request.

## Previous continuation: optional feature gates and faster media feedback (2026-09-13)

Implemented the owner's request for missing-environment checks and graceful optional-feature disablement. `core/environment.ts` now treats only `TOKEN` as essential for startup, independently checks database/AI/memory/birthday-reminder/Giphy/social/private-forwarding configuration, and emits fixed-reason variable-name-only startup diagnostics through the shared logger. With missing database settings the bot does not connect, track activity, schedule reminders, run social jobs or forward Discord logs; persistence-backed commands/autocomplete return disabled/empty responses without querying. Basic commands and configured AI chat remain available. Missing AI settings block both startup enablement and `/toggleai`; missing memory settings let chat work without context/embedding/storage calls; missing Giphy settings skip HTTP entirely. Destination/timezone issues disable only reminders. Existing configured-service outage/retry behavior remains unchanged. See [feature configuration](feature-configuration.md) and the updated `.env.example`/README.

Media admission no longer waits for message-activity storage, and accepted jobs wake the scheduler immediately. The queue drains due jobs without the old five-second gap while retaining one active worker; idle/error/readiness polling stays five seconds. No download, decode, isolation, permission or durable-send checks were removed. Active extraction/send shows the native expiring Discord typing indicator as cooking feedback, refreshed every eight seconds; requests never overlap, failures/timeouts stop feedback without failing the job, and completion/cancellation/shutdown stop refresh. No temporary reply/reaction or status-message database schema was added. Content-free extraction/send timing logs support further latency diagnosis. Downloads, provider latency, container startup/verification and upload can still take time.

Verification: `npm run check` passes **297 tests, one optional PostgreSQL skip (298 total)**. Fifteen new tests cover feature isolation and token-only startup, no-query/no-GIF paths, blocked toggles/commands/autocomplete, memory-free AI, immediate wake/draining/single-flight scheduling, indicator failure/timeout/cancellation, and timing diagnostics. Existing tests were updated for optional configuration and concurrent activity admission. The production build includes `socialProgress.js`. No external test messages, metadata/media probes, historic job replays or manual database changes were performed for this continuation.

Live local activation: identity/cwd-checked bot **19789** stopped gracefully; updated bot **50640**, session **18268**, started **17:38:52 UTC**, with `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test SOCIAL_MEDIA_ENABLED=true SOCIAL_WORKER_SOCKET=/tmp/caitlyn-owned-api.lThfz1/worker.sock LLM_ENABLED=false LOG_LEVEL=INFO`. Startup logs confirmed the configured features and AI disabled. Broker **94635**, account-capable FxEmbed container, key file and images remain unchanged; broker health was idle/ready before restart. Verify process identity/cwd before future stops. Use a fresh owner-sent X link to check perceived latency and the indicator in Discord.

Nothing installed. No new slash-command JSON or registrations, migrations, commits/merges/pushes, autostart or homeserver changes. Existing file headers/folders were preserved; new documentation is under `docs/` and README remains at the root.

## Previous continuation: cleaner X preview presentation (2026-09-13)

The owner confirmed the authenticated video path is working and requested embed-style presentation without a visible source URL. `core/socialLinks.ts` now shares URL spans between admission and caption cleanup. New messages keep `Shared by @sender` and the source caption, but omit visible handled X post links (including repeated aliases/tracking/video suffixes). Named Markdown labels remain, and unrelated/hidden/suppressed/unsupported links and links beyond the admission limit are retained to avoid losing source context. The original X post remains linked by the embed title/author. The renderer uses `View post on X`, cleans plain author names without literal Markdown escaping, and removes empty-text filler on media-only cards. Verified MP4 attachments, image galleries, quote attribution, full-text fallback and durable job footers/source-deletion guards are unchanged.

`npm run check` passes **282 tests, one optional PostgreSQL skip (283 total)**. Seven added tests cover caption cleanup/admission limits, standalone attribution, multi-link captions, clickable author/title fields and media-only cards. No live test message was sent or existing preview edited; the owner should send a fresh link to check the new layout.

After verifying the old process identity/cwd, stopped bot **56725** gracefully and started the updated bot as **19789**, session **62013**, at **15:20:17 UTC** with local `caitlyn_test`, existing `/tmp/caitlyn-owned-api.lThfz1/worker.sock`, social integration on and AI off. The account backend and broker **94635** remain unchanged (see the account activation checkpoint below). Verify these identities before future stops. Nothing installed, no new commands/registrations or migrations, and no commit/merge/push or homeserver changes.

## Previous continuation: .env account activated and prior video verified (2026-09-13)

The owner filled the three local `.env` fields and requested continuation. Validated the values in-process without displaying them; `.env` remains owner-only (0600), Git/Docker excluded, and unchanged by this activation. No 1Password CLI was used. Activated the existing account-capable FxEmbed image through `manage.ts start-env`; the encrypted session/key entered the container over stdin, not Docker settings, arguments, image layers or logs.

The previously age-denied post (`minakonha`, ID `2093066016301457704`) now succeeds through the live private broker. Metadata returned **ready**, 35 text characters, one video with four variants, no quote and no issues. The subsequent full media check returned **ready**, one verified video attachment of **1,769,698 bytes**, no media failures, zero omitted media, and a complete one-embed renderer payload. A public text regression (`MasterLeytrx`, ID `2097877698894770267`) returned ready / 163 characters at **14:57:52 UTC**. These probes sent no Discord messages, did not alter source messages and did not replay any historical job. This verifies this post with the current session; it is not a guarantee that every restricted/unavailable post is accessible.

Current live state (verify identity/cwd again before any future stop):

- Bot remains PID **56725**, session **44649**, unchanged and using the existing private socket/local DB/AI-off settings. No second bot was started.
- Old broker **79308** stopped gracefully; new broker **94635**, session **73379**, ready **14:55:41 UTC**, `mode=fxembed`, `localApiAuth=true`, cwd this checkout.
- Socket `/tmp/caitlyn-owned-api.lThfz1/worker.sock`; private local API key `/tmp/caitlyn-owned-api.lThfz1/fxembed.key` (0600, private parent). The key is separate from the X session and is not recorded here.
- New active container `caitlyn-fxembed`, ID **3f13f655f123ac26a964abe69871bc2695f527736d7e6665aa3284b5de60922f**, image **sha256:8892d8b6cd000c8fae5f612a5e3c3fb09d687f206cced55ae7a12cd07d0a3768**.
- Old anonymous container **0d2ac6e5141351055b0837826a6452374d81695069115e702147d623a52dd199** is stopped and retained as `caitlyn-fxembed-anonymous-backup`, original image **sha256:2ad432736e361f74eb33eb99f360692d45735446667ddf0816675ed9fb2ea864**. Nothing was deleted for rollback.
- Docker context remains `colima-caitlyn-media-test`; verifier remains `caitlyn-media-api:local` = **sha256:e63b987a52171befed7615eff7cfbe41bb5d01e27bcc4155f957a336399b0870**.
- Confirmed local health returns **401 without the private key**, **200 / ready=true / accountConfigured=true** with it. Container root is read-only, UID 1000:1000, logging driver none, no mounts, only published address 127.0.0.1:8787. Docker environment has no account/encryption/API-key variables. Private tmpfs runtime config is 0600; no raw Wrangler log file exists.
- Verified the disposable media containers and IPC volume were removed after the check. Bot and broker each have one running process in this checkout.

Broker startup now requires the following extra key-file setting; only the path is passed in the environment:

```bash
SOCIAL_WORKER_DOCKER_CONTEXT=colima-caitlyn-media-test \
SOCIAL_WORKER_IMAGE=caitlyn-media-api:local SOCIAL_X_PROVIDER=fxembed \
SOCIAL_FXEMBED_URL=http://127.0.0.1:8787 \
SOCIAL_FXEMBED_KEY_FILE=/tmp/caitlyn-owned-api.lThfz1/fxembed.key \
SOCIAL_WORKER_SOCKET=/tmp/caitlyn-owned-api.lThfz1/worker.sock \
LOG_LEVEL=INFO node --import tsx scripts/socialWorker/server.ts
```

Nothing installed or rebuilt; reused existing image/dependencies. No new commands/registrations, migrations, manual DB changes, commits/merges/pushes, autostart or homeserver actions. Next is a **fresh owner-sent X link in the opted-in Discord channel** to verify actual desktop/mobile playback, sender mention and complete-source replacement. Existing failure/partial/uncertain handling continues to preserve the original. If the X session expires, update the local `.env` values and recreate the backend with `start-env`; a bare container restart does not restore its temporary secret configuration.

## Previous continuation: local .env account setup (2026-09-13)

At the owner's request, local account startup now uses the root `.env` directly, without 1Password CLI prompts. Added blank `FXEMBED_X_USERNAME`, `FXEMBED_X_AUTH_TOKEN` and `FXEMBED_X_CT0` fields to the actual git-ignored `.env` and to `.env.example`; all pre-existing values were preserved. Tightened `.env` permissions from 0644 to 0600. **The new fields are still blank.** No account values were fetched/copied from 1Password in this continuation and no real X account is active yet. The owner must paste the handle without `@` and the two complete session cookie values directly into that local file, never chat.

New `scripts/fxEmbed/envSession.ts` validates a bounded owner-only non-symlink `.env`, rejects missing/malformed/duplicate X fields, and selects only the three account values without populating `process.env`. `manage.ts start-env` uses the existing encrypted-stdin/private-tmpfs/authenticated-local-API path; no backend image changes are needed. Docker CLI children explicitly exclude `FXEMBED_X_*`. Centralized normal dotenv loading in `core/loadEnvironment.ts` and routed existing imports through it so bot, broker, command deployment and diagnostics preserve ordinary settings/overrides but exclude X session fields from their environment and child processes. The `.env` source is intentionally plaintext on the host; file permissions/ignore rules do not protect it from the same user or an administrator. Full instructions and the optional old 1Password route are in [fxembed.md](fxembed.md).

Verification adds six regression tests for quoted/CRLF values, unrelated-secret exclusion, missing/duplicate/malformed values, private file checks, process overrides, configured dotenv paths, and Git/Docker exclusions. `npm run check` passes **275 tests, one optional PostgreSQL skip (276 total)**. Nothing installed, no new image built, no new slash commands or registrations, no commit/merge/push, and no homeserver changes.

Live bot/broker/backend and their identities below are unchanged; backend health still reports ready=true / accountConfigured=false. The account-capable candidate remains tagged `caitlyn-fxembed:local` (`sha256:8892d8b6cd000c8fae5f612a5e3c3fb09d687f206cced55ae7a12cd07d0a3768`), while the original anonymous image is still running. No host API key was created. Once the three fields are filled, validate them without displaying values, then perform the identity-checked, rollback-preserving backend/broker switch described below, using this command instead of `start-account`:

```bash
node --import tsx scripts/fxEmbed/manage.ts start-env \
  colima-caitlyn-media-test /tmp/caitlyn-owned-api.lThfz1/fxembed.key
```

1Password access is no longer a blocker or local runtime dependency. Do not keep retrying the vault; use the owner's `.env` selection. Restart the broker with the corresponding `SOCIAL_FXEMBED_KEY_FILE`, then test the approved previously failing post metadata-first; preserve old jobs and leave the homeserver inactive. Account authentication, restricted-post media delivery and fresh Discord playback/source replacement remain unverified until that setup.

## Previous continuation: secure FxEmbed account support, activation pending (2026-09-13)

The owner created a dedicated X account, confirmed the previously failing post is visible when signed into it in Dia, and designated 1Password vault `personal`, item `X-Caitlyn`. This authorizes using that item's session for local FxEmbed. Never ask for cookies in chat or print them. One bounded CLI read succeeded and printed **only field names/types/presence**; subsequent reads timed out after 45 seconds. The item has both `username` and `Username` fields, plus concealed `auth_token` and `ct0`. Actual field-format validation has not been established: the validation command returned a generic failure and later diagnostic reads timed out. Do not infer missing/invalid cookies from those timeouts. The operator must unlock/approve CLI access before continuing.

Implemented `scripts/fxEmbed/credentials.ts` (one designated item, strict fields, in-memory AES-256-GCM), `localAccess.ts` (owner-only bounded regular API-key file), `bootstrap.mjs` (stdin-only configuration into private container tmpfs, quiet Wrangler), and `service.mjs` (local API authentication before account initialization/upstream I/O, sanitized headers, no-store responses). The build maps upstream encrypted-payload reads to runtime values; no secrets are baked into images and all vendored source stays unchanged. `manage.ts start-account` performs the opt-in setup, refuses an existing backend container, and removes only its own newly created container on startup failure. The broker accepts `SOCIAL_FXEMBED_KEY_FILE`, logs a boolean `localApiAuth`, and never receives X cookies. Missing/decryption-invalid configuration fails closed. Upstream still owns normal X request/retry behavior; decrypted local configuration is not proof of X acceptance.

Verification: `npm run check` passes **269 tests, one optional PostgreSQL skip (270 total)**. Nine new tests cover parsing/encryption/tampering, bootstrap validation, private file permissions/symlinks, local authentication, stripped headers, honest health, fail-closed initialization, and authenticated broker headers. A temporary real container on loopback port 8788 received an encrypted **synthetic** account via stdin: unauthenticated health returned 401, authorized health returned 200 / accountConfigured=true, using actual bundled upstream decryption. Runtime config was mode 0600, no Wrangler log file was created. No request to X or Discord was made by that check. Test container `2c9d9589d352527146354a542d40529a81e111894fbe9cb252de223da15872e9` and its tmpfs were removed; no important data was stored there.

Built candidate tag `caitlyn-fxembed:local` = **sha256:8892d8b6cd000c8fae5f612a5e3c3fb09d687f206cced55ae7a12cd07d0a3768**. **Nothing installed**: all 12 base/dependency layers match the prior image; only two integration/build layers changed. Existing `/opt/homebrew/bin/op` version 2.39.0 was reused. Installation inventory remains unchanged. No commits/merges/pushes, new slash commands, registrations, migrations, `.env` changes, autostart or homeserver actions.

**Live state has NOT changed**: bot PID 56725, broker PID 79308, socket `/tmp/caitlyn-owned-api.lThfz1/worker.sock`, original anonymous FxEmbed container `0d2ac6e5141351055b0837826a6452374d81695069115e702147d623a52dd199` still runs image `sha256:2ad432736e361f74eb33eb99f360692d45735446667ddf0816675ed9fb2ea864`. The tag now refers to the new candidate, not that running image. No real account was configured in a container, no host API-key file was created, and the age-restricted post has NOT been re-tested or fixed yet.

Next, with CLI access approved: validate the designated item's three values in-process without printing them (resolve any `username`/`Username` ambiguity without exposing values). Verify broker idle, process cwd and container identity; gracefully stop the broker, stop/rename the old anonymous container for rollback, and run:

```bash
node --import tsx scripts/fxEmbed/manage.ts start-account \
  colima-caitlyn-media-test personal X-Caitlyn /tmp/caitlyn-owned-api.lThfz1/fxembed.key
```

Restart only the broker with its existing environment below plus `SOCIAL_FXEMBED_KEY_FILE=/tmp/caitlyn-owned-api.lThfz1/fxembed.key`. Bot stays running, AI off, local DB unchanged. Confirm unauthenticated local requests fail, authenticated health is ready, then test the owner-approved previously failing post (`minakonha`, ID `2093066016301457704`) metadata-first through the private broker. If successful, test bounded verified attachment delivery without sending to Discord, followed by a fresh owner-sent message for playback/source cleanup. Do not replay historical jobs. If account activation fails, restore the old anonymous backend/broker rather than leaving a partial switch. Full setup/trust boundaries are in [fxembed.md](fxembed.md).

## Previous continuation: replace VX with local FxEmbed (2026-09-13)

The owner requested completely removing VX and using FxEmbed. The project now contains 295 byte-identical upstream runtime/locale/package files under vendor/fxembed, pinned to **5b5b6207d9fd93bae15a68ab659fbc97cdbf61b5**. Every vendored Git blob was compared with that upstream tree; all match. [Source manifest](fxembed-source-manifest.json), [MIT notice](licenses/FxEmbed.txt), [integration/setup](fxembed.md), and [complete exact installation inventory](fxembed-installations.md) are in docs.

Removed the VX-derived Python client/request profiles, VX hosted-provider mode, VX normalizer, old session-file support, unused legacy message handler and their obsolete tests/docs. Removed source files are recoverable from /tmp/caitlyn-vx-rollback.figN5v/removed-files.tgz (private temporary archive, not a durable backup). Older images were retained, not pruned. The VX sections below are archived history, not current configuration instructions.

The sole/default provider is **fxembed**. The broker calls loopback FxEmbed API v2, validates bounded metadata/identity/quotes/CDN URLs, and sends JSON to the network-none verifier on stdin. Metadata-only requests do not launch media containers. UTF-8 buffering is tested across split multibyte characters. Existing verified Discord uploads, sender mentions, private logs, opt-ins, durable jobs and conservative original-message deletion remain unchanged. No new command definitions/registrations, migrations, account connection, commit, merge, push or homeserver deployment.

FxEmbed runs actual upstream worker/Atmosphere code in local Wrangler/workerd. Two build-time cache hints are adapted for local workerd's cache-options conflict, with a strict match-count guard; vendored source remains unchanged. Node's standard Mozilla CA roots supply the slim image's missing TLS trust store; certificate verification stays enabled. The wrapper exposes only X post metadata and health, suppresses raw upstream content/token logs, and disables hosted helper services/telemetry/credentials. See the guide for backend networking/trust boundaries and the current arm64 image limitation.

Verification: npm run check passes **260 tests, one optional PostgreSQL skip (261 total)**. All five offline Python media-request tests pass. The final verifier image passed a native Node Unicode-stdin smoke test. Real public checks passed: text 163 characters; image PNG 398,830 bytes; gallery two JPGs totaling 76,543 bytes; video MP4 3,162,626 bytes; prior public regression MP4 2,652,337 bytes; quote two independently attributed MP4s totaling 1,348,582 bytes. Every media case passed worker/client/renderer with zero omissions. No smoke test posted to Discord. The previously age-denied minakonha post was not re-probed or replayed. No account is configured; restricted/NSFW access remains a separate authorized setup and verification step. Manual desktop/mobile playback and source cleanup need fresh owner messages.

Current local state (verify identity/cwd before stopping anything):

- Bot PID **56725**, exec session **44649**, online **12:36:20 UTC**.
- Broker PID **79308**, exec session **24586**, ready **12:38:00 UTC**, provider fxembed. Prior bot 2897 and brokers 2552/46041 are stopped.
- Socket: /tmp/caitlyn-owned-api.lThfz1/worker.sock (directory 0700, socket 0600, owner raze).
- Docker context: colima-caitlyn-media-test.
- FxEmbed container: caitlyn-fxembed, ID 0d2ac6e5141351055b0837826a6452374d81695069115e702147d623a52dd199.
- FxEmbed image: sha256:2ad432736e361f74eb33eb99f360692d45735446667ddf0816675ed9fb2ea864. Only published port **127.0.0.1:8787**; read-only root, UID 1000, no Docker logs, no host mounts/secrets.
- Verifier tags caitlyn-media-api:local and caitlyn-media-fxembed:local: sha256:e63b987a52171befed7615eff7cfbe41bb5d01e27bcc4155f957a336399b0870. All seven existing dependency/base layers reused.
- Broker environment: SOCIAL_X_PROVIDER=fxembed, SOCIAL_FXEMBED_URL=http://127.0.0.1:8787, SOCIAL_WORKER_IMAGE=caitlyn-media-api:local, SOCIAL_WORKER_DOCKER_CONTEXT=colima-caitlyn-media-test, socket above, LOG_LEVEL=INFO.
- Bot overrides: PGHOST=127.0.0.1, PGPORT=5432, PGDATABASE=caitlyn_test, SOCIAL_MEDIA_ENABLED=true, LLM_ENABLED=false, LOG_LEVEL=INFO, same socket. The .env file is unchanged. Exactly one bot and one broker verified.
- Final metadata request through the live broker returned ready / 163 characters / provider fxembed; no media downloaded. Broker health idle, no leftover disposable media containers/IPC volumes. Backend health checks local runtime/cache, not X/account access.
- Installed only inside the FxEmbed container: 436 present npm package paths plus workspace, Node 24.19.0, npm 11.17.0, bundled Yarn 1.22.22 and 88 Debian base packages, all itemized in the inventory. No host/root-project/Python packages or autostart added.

## Historical: removed VX-derived X client (2026-09-13)

This section is archived history. Its code, provider choices and account-session instructions were superseded by the FxEmbed replacement; do not use these restart commands.

The owner requested that VXTwitter's retrieval behavior become part of Caitlyn, after clarification that the previous self-hosted API still wrapped yt-dlp. Implemented `scripts/mediaSandbox/xClient.py` using Python stdlib and `xApiProfiles.py` with request profiles adapted from BetterTwitFix commit `c1d3a7e2ddaf8181978dba449665bc3fba0e7b36`. License/attribution is under `docs/licenses/`. `xMetadata.py` now uses this client, not yt-dlp. See [integrated X extractor](fxembed.md) for exact retained behavior, differences, boundaries and session configuration. No hosted VX request occurs in direct mode, and no anonymous-success preflight is required for the optional authenticated path.

Guest-only operation uses VX's pinned REST-result profile. Optional operator-configured session operation starts authenticated and has up to three bounded compatibility methods (batch, REST result, conversation). Fixed endpoints, request deadlines/JSON caps, no redirects, no account rotation, no retries after actual denials/rate limits, and exact post selection remain enforced. The normalizer still rejects protected/subscriber-only responses. Sender mentions, verified attachments, complete-replacement deletion, durable jobs, private log routing and AI-off behavior are unchanged. There is no persistent metadata/media cache or full VX website/server installation.

`SOCIAL_X_SESSION_FILE` is a broker-only optional path, loaded through a bounded owner/private/non-symlink regular-file check. A configured session travels through Docker stdin and Python stdin, never API request bodies, environment, argv, bind mounts or logs. The bot/gateway/verifier/media-download requests receive no session. **No account was configured or inspected.** Real authenticated extraction is unverified; synthetic tests cover request profiles/fallbacks and credential handling. Obtain explicit owner approval before accessing or configuring any account. Never request that tokens be pasted in chat.

The latest reported post (`minakonha`, ID `2093066016301457704`) was checked once through the new guest client, metadata only. It now returns an explicit age-restriction notice: `restricted`, `TweetTombstone`, `age_required`, `source=tombstone_text`, `hasLegacy=false`, `hasTombstoneText=true`. This is stronger evidence than the earlier unexplained bare tombstone. No media was returned, downloaded, posted or deleted for this check. **Do not claim that post is fixed.** The remaining user decision is whether to configure a dedicated authorized X account that can already view the post, then verify authenticated retrieval. Do not replay historical jobs or try other anonymous endpoints after the explicit notice.

Verification: `npm run check` passes **265 tests, one optional PostgreSQL skip (266 total)**; **14 new integrated-client Python tests + eight existing media-request tests** pass offline in the existing image runtime. Cases cover no guest preflight with a session, all three session response shapes, compatibility fallback, exact identity, wrappers/quotes, terminal denials/rate limits, bounded bodies/deadlines, header isolation, malformed inputs, private file permissions and content-free private logs. Public text (163 characters), PNG (398,830 bytes), MP4 (3,162,626 bytes), two-image gallery (49,034 + 27,509 bytes), and independently attributed quoted MP4s (786,568 + 562,014 bytes) passed live direct-client checks. All four media cases passed worker/client/renderer with zero omissions. Probes sent no Discord messages.

Installed packages: **none**. Offline source-only image **`sha256:ef79020f8ac278ba464239771345785c34f3f73671786e910c67e64910878e3b`**, tagged `caitlyn-media-integrated:local` and promoted to `caitlyn-media-api:local`; all seven dependency/base layers reused. Old images remain available by digest; no images/dependencies pruned. Temporary build contexts, probe containers/media/IPC, prototype socket and its empty `/tmp/caitlyn-x-integration.OPWWZH` directory were removed. The existing test VM remains running.

Gracefully stopped and verified exit of old bot **95581**, old broker **95352**, and prototype broker **37640**. **Current broker PID 2552**, session **94250**, ready **2026-09-13 11:24:12 UTC**. **Current bot PID 2897**, session **6574**, online **11:24:16 UTC**. One bot and one broker run from this checkout. Socket stays **`/tmp/caitlyn-owned-api.lThfz1/worker.sock`**, private parent 0700/socket 0600. Broker uses `SOCIAL_X_PROVIDER=direct`, `SOCIAL_WORKER_IMAGE=caitlyn-media-api:local`, context `colima-caitlyn-media-test`; `SOCIAL_X_SESSION_FILE` was explicitly omitted. Bot uses process-only `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test SOCIAL_MEDIA_ENABLED=true LLM_ENABLED=false LOG_LEVEL=INFO` and the same socket. `.env` unchanged. Verify PID/cwd before stopping services.

No new slash commands, migrations, manual database changes, commits, merges, pushes, or homeserver changes. All 14 existing command definitions remain registered; no command publication was needed. Remaining work: owner-approved X account configuration/authenticated validation, manual Discord playback/source-cleanup confirmation, and previously recorded production supervision/platform adapters.

## Previous continuation: authorized local Discord test (2026-09-12)

### Previous: initial Caitlyn-owned self-hosted media API

The owner approved self-hosting and asked to minimize third-party dependencies, and asked whether media is downloaded then uploaded to Discord. Confirmed that verified bytes are downloaded into temporary worker storage, passed in memory, and uploaded as Discord attachments. The disposable downloads are removed; Discord keeps the delivered copies. Full behavior, API contracts, retained dependencies and build instructions are in [self-hosted media API](self-hosted-media-api.md).

After inspecting upstream VX, explained the implementation choice: use a **Caitlyn-owned private API around the existing local extractor**, not a literal full VX website/backend clone. This avoids adding Flask, Nginx, MongoDB, translation services or another web stack. The pinned yt-dlp library remains the low-level public X request dependency, and FFmpeg remains the local verifier. No hosted embed API is used by the current mode. Existing private logging, sender-only mentions, byte limits, captions and complete-replacement deletion rules are unchanged. Actual X restrictions remain terminal; the previously blocked post was not queried or fixed by this work.

Added `POST /v1/x/metadata` with direct-only extraction and no media downloads, plus a local-only `GET /v1/health` liveness endpoint. The existing `POST /v1/x` returns verified media for the bot to upload. Metadata has its own strict request/result types, one-MiB response bound, safe CDN URL/quote handling, double server/client validation, and `purpose=metadata` separation so it cannot authorize delivery/deletion. Provider/operation flags in request bodies cannot select another route or runtime. Health works while busy and never probes Docker/X/Discord/DB. The default direct gateway denies VX; the broker requires proof of that denial in the per-job isolation check. Hosted VX remains a manual legacy opt-in only, not an automatic fallback.

New reproducible scripts: `scripts/buildSocialWorkerImage.ts` creates an offline source-only image using an explicit file allowlist, cached pinned base, unchanged dependency layers and tag-after-verification; `scripts/testSocialMetadataLocal.ts` prints content-free metadata counts from a running API. The builder does not bootstrap a fresh machine or install dependencies. `.env.example`, README and docs describe direct/self-hosted defaults. No new slash commands or registration needed; 14 definitions remain unchanged.

Verification: `npm run check` passes **260 tests with one optional PostgreSQL skip** (261 total); 11 new API tests plus updated gateway tests cover route validation, metadata-only retrieval, terminal failures, purpose separation, safe URLs/quotes, busy liveness, response bounds and cancellation. Eight Python request-safety tests and a native container smoke check pass offline. A separate prototype broker was tested while the working bot stayed unchanged: public text returned 163 characters with no downloads; image 398,830-byte PNG; video 3,162,626-byte MP4; gallery 49,034/27,509-byte JPGs; quote two independently attributed MP4s of 786,568/562,014 bytes. All four media cases passed real worker/client/renderer checks with zero omissions. No probe posted to Discord. A final metadata check after switching/restarting passed the strict per-job no-VX isolation requirement. Manual Discord playback/source cleanup still needs fresh owner messages.

**Nothing installed.** Built `caitlyn-media-api:local` as **`sha256:ee9c1a41464519d86d385a4eb0b6c04b1143bc551a9793b9b3293aaa1219c5c7`**, reusing all seven dependency/base layers. The prior `caitlyn-media-probe:local` tag/image remains intact for rollback/reference. Owned temporary build directories, probe media, containers and IPC volumes were removed. The existing Colima VM remains running; no public service or autostart was added.

Gracefully stopped and verified exit of prior bot PID 66442, prior VX broker PID 66205, and prototype broker PID 51451. **Current broker PID 95352**, session **37426**, ready **17:23:58 UTC**; **current bot PID 95581**, session **47432**, online **17:24:02 UTC**. Both run from this checkout; exactly one bot and one broker remain. New active socket: **`/tmp/caitlyn-owned-api.lThfz1/worker.sock`**, directory 0700/socket 0600, owner raze. Broker uses `SOCIAL_X_PROVIDER=direct`, `SOCIAL_WORKER_IMAGE=caitlyn-media-api:local`, and `SOCIAL_WORKER_DOCKER_CONTEXT=colima-caitlyn-media-test`. Bot uses the same new socket, `PGHOST=127.0.0.1 PGPORT=5432 PGDATABASE=caitlyn_test`, `SOCIAL_MEDIA_ENABLED=true`, `LLM_ENABLED=false`, `LOG_LEVEL=INFO`. `.env` remains unchanged; these are process overrides. The old socket was removed by its stopped broker; its empty parent directory was left alone. Verify PID/cwd identity before any later stop.

No migrations, manual DB edits, command publication, commits, merges, pushes, or homeserver changes. Next: fresh manual public X posts to verify Discord playback, mention/source replacement and private `mode=direct` logs. Do not replay historical failed/sent jobs or claim unrestricted X access. Production service supervision, fresh-host dependency provisioning and remaining platform adapters remain separate work.

### Previous: public VXTwitter content provider

The owner approved using VXTwitter for content retrieval, after being told that explicit age/login restrictions would not be bypassed. Implemented operator-owned `SOCIAL_X_PROVIDER=direct|vxtwitter` on the separate broker; omitted settings remain `direct`, and invalid values fail before Docker access. The active local test now uses `vxtwitter`. The deprecated `messages/twitterVideoMessage.ts` was not reactivated. See [provider behavior/configuration](social-delivery.md#vxtwitter-content-provider).

The isolated worker first normalizes the existing public X response; a restricted, unavailable/tombstone, invalid, or failed check stops before VX. Only publicly available parent/quote IDs can be queried, with at most two bounded VX requests and no nested/unknown quote fetches. VX supplies post text/author/media URLs, while the existing renderer, sender-only mention, verified attachments, and complete-replacement deletion policy remain intact. Missing identities/media/text, unsupported cards, and malformed quotes remain failures or partial results. Access denials halt downloads and never switch providers. Broker mode is validated at transport and private logging boundaries; raw provider data stays out of logs.

Live checks used only previously supplied public examples: text returned 163 characters and `ready`; image returned a verified 398,830-byte PNG. The first video check exposed VX's single highest-quality MP4 exceeding the 8 MiB upload budget. Fixed this by retaining smaller, already-public variants for the same media ID from the initial X response, without extra metadata requests or changing limits. The follow-up video returned a verified 3,162,626-byte H.264/AAC MP4, `ready`, complete renderer output, and zero omissions. These checks exercised real isolated extraction and host decoding; the video also exercised the renderer. No Discord preview was sent by the probes. Manual desktop/mobile playback remains to be checked. **The previously blocked minakonha post was not queried through VX, is not fixed by this mode, and must not be automatically replayed.**

Verification: `npm run check` passes **249 tests, one optional PostgreSQL skip** (250 total). Added 16 tests for VX normalization, provider selection, public preflight, quote bounds, video budgets, exact hostname egress, Unix transport, and private logs. All eight Python request-safety tests pass without network access (three new VX tests), and container-native module import/normalization/preflight smoke checks pass. No packages installed. Source-only image **`sha256:f51fafe36fd921f879b785faa2d3731328e65fd117a21f6a6dbff3f5eebd43b1`** reuses all seven dependency/base layers. Owned probe containers, media, IPC volumes, and temporary build directories were removed; the existing VM remains running for manual testing.

Gracefully stopped prior bot PID 35428 and broker PID 188, verified exit, then restarted after testing. Current broker PID **66205**, session **49464**, ready **16:59:03 UTC** in `mode=vxtwitter`. Current bot PID **66442**, session **24276**, online **16:59:07 UTC**. Exactly one local bot and one broker run from this checkout. The socket remains `/tmp/caitlyn-discord-test.uEUE6g/worker.sock` (directory 0700/socket 0600, owner raze). Same process-only local DB, social-enabled and AI-disabled overrides; `.env` unchanged. Verify process identity/cwd before stopping anything. To retain VX on restart, export `SOCIAL_X_PROVIDER=vxtwitter` for the broker as documented; this is not a bot `.env` toggle.

No new commands or registration required; the same 14 command definitions remain. No migrations, manual DB edits, commits, merges, pushes, or homeserver changes. Next: repost fresh public test links in the opted-in channel, verify playback/sender/source cleanup and `mode=vxtwitter` in private logs. Preserve failed/sent job history rather than replaying old jobs. Production supervision and remaining platform adapters remain separate unfinished work.

### Previous: X metadata parser audit and safe diagnostics

The owner approved a local parser audit after asking how EmbedEZ handles age-restricted posts. Research established that EmbedEZ describes server-side media re-serving, but its published SDK only calls its service and the public extractor repository contains templates. Its actual X acquisition method was not established; **do not claim authenticated access is the only possible implementation or that this post is definitely age-gated**.

Compared public yt-dlp/Nitter response handling, then independently added terminal metadata diagnostics to `core/socialXPost.ts`: known structured reasons, bounded fixed notice fields (`text`/`richText`), explicit unknown tombstones, malformed responses, and identity mismatch. Fixed outer tombstones being lost when unwrapping visibility results, honored newer `user.privacy.protected`, and classified subscriber-only `TweetPreviewDisplay` as restricted. Actual access denials remain terminal; no alternative endpoints, authentication, media fallback, or new provider requests were introduced. This improves parsing/error reporting, **not a verified fix for the failed video**.

The worker already returns parser failures unchanged. The client now retains only strictly validated diagnostic enums/booleans, and runtime validates again before adding them to the existing private guild-scoped warning. Raw provider text/URLs/extra fields cannot be logged. Older workers without diagnostics remain compatible. Failure handling still preserves the original and never sends/deletes/retries a terminal metadata failure. Detailed contracts and upstream references are in [X metadata diagnostics](social-delivery.md#x-metadata-diagnostics).

Verification: `npm run check` passes **233 tests, one optional PostgreSQL skip** (234 total). Added 12 tests covering notice variants, unknown/hostile/malformed data, structured reason precedence, wrapper/privacy fixes, Unix transport and runtime logging/source preservation. Seven synthetic checks plus worker-module loading also passed inside the existing isolated image, with zero provider requests. The failed post's previous bare `TweetTombstone` shape is reproduced offline and remains `unavailable` with `unknown_tombstone`. No new media retrieval or live Discord preview was attempted during the audit.

Source-only image: **`sha256:fac668a2dec3cee210db7b168d7f60c57cae438bbc44b859762f32a587f9643f`**, all seven dependency layers unchanged; **nothing installed**. The temporary build directory/test container were removed. Gracefully stopped and verified exit of bot PID 10843, then broker PID 10462. Intermediate bot PID 4143 was also stopped after the final client validation check (array-valued outcomes must be rejected, not string-coerced). Current broker PID **188**, session **60807**; current bot PID **35428**, session **32344**, online at **16:26:27 UTC**. Both run from this checkout. Socket remains `/tmp/caitlyn-discord-test.uEUE6g/worker.sock` (directory 0700, socket 0600). Same process-only local DB/social-enabled/AI-disabled overrides; `.env` unchanged. The dedicated VM is intentionally still running. Verify PID identity/cwd before stopping anything.

No migrations, database content edits, command definitions/registration, commits, merges, pushes, or homeserver changes. All 14 registered commands remain unchanged. Next: use the new diagnostic on future owner-supplied test messages to distinguish explicit provider denial from unknown/malformed metadata; do not automatically replay old failed or sent jobs, repeatedly query denied posts, or promise an age-gated video fix.

### Previous: remove the application-imposed channel-age restriction

Follow-up after the owner reported it still fails: the new job `e141e321-9588-4143-9357-92448759fd62` for the same minakonha post failed at 15:57:31 UTC, attempt 1, `unavailable`, no Discord message ID. A metadata-only diagnostic using the same X GraphQL call through a fresh isolated gateway confirmed a **`TweetTombstone` response with no legacy post/media data**, not a Discord upload failure. No media download or alternative provider endpoint was attempted. The response did not expose a recognized reason code, so do not assert a specific age/deletion cause as proven. Further support for genuinely age/login-gated posts requires a user-authorized X access method/account; no such account has been configured or authorized. Diagnostic containers/IPC were removed, nothing installed, and the existing bot/broker remain running unchanged.

The owner explicitly said not to require an age-restricted Discord channel. Removed that extra check from source validation, sending, and confirmed source cleanup; runtime worker requests now always opt into public sensitive-labelled metadata. Sensitivity is still validated/recorded for parent and quoted posts. Actual X protection/login/age-verification gates remain untouched. Sender-only mentions, captions, and safe original deletion are unchanged. Channel settings themselves were not changed.

Ran one bounded worker check for the failed post in the local DB (`https://x.com/minakonha/status/2093066016301457704`) with `allowSensitive=true`: outcome remains `unavailable`, with no media returned or Discord send. This is a separate access/availability issue; do not claim it is fixed or definitely caused by age verification. Ask whether it opens when the owner is logged into X before proposing authorized account access. No cookies, alternate endpoint, or denial-bypass method was used.

`npm run check` passes 221 tests with one optional PG skip; targeted transport/runtime/delivery tests pass all 34 after the final test-label update. No migration, installation, command-schema change, registration, commit, merge, push, or homeserver change. Gracefully stopped bot PID 13137 and verified it exited. Current bot PID **10843**, online at **15:54:19 UTC**; broker remains **10462**, with the same private socket and image noted below. Exactly one local bot is running; AI remains off and local social testing remains enabled through process overrides.

### Previous: sensitive labels, sender mentions, and safe source removal

The owner requested sensitive/age-restricted X support, removal of originals to avoid duplicates, and a mention of the original sender. Implemented support for **public metadata marked `possibly_sensitive` only when the destination is an age-restricted Discord channel**. Explicit X age/login/protection denial is still blocked. Parent/quoted sensitivity is preserved across the worker protocol and checked again immediately before send. The existing test channel `1152942687603933285` is not age-restricted; no channel access settings were changed. A failing example was requested asynchronously but has not been supplied; do not claim actual X age-gated access works. A read-only DB check found an `unavailable` job for `https://x.com/minakonha/status/2093066016301457704`, which by itself does not prove the cause.

New previews are standalone posts containing `Shared by <@sender>` and the original caption/links, with allowed mentions limited to that sender. Complete previews can replace their unchanged source after every expected sibling X job has confirmed delivery. Missing permissions, extra uploads/stickers/polls/reply/thread context, overlong source text, or partial extraction preserve the original. Migration `016` durably separates source-deletion intent from user edit/delete cancellation; self-delete events and lost acknowledgements must never cascade into deleting successful replacements. Existing jobs keep their originals and are never retroactively cleaned up. Detailed contracts and race limitations are in [social delivery](social-delivery.md).

Applied `016` transactionally to verified local `127.0.0.1:5432/caitlyn_test`: all four pre-existing jobs are `preserve`, and all original fields match their fingerprints. Current broker PID **10462**, bot PID **13137**, online at **13:40:40 UTC**. Verify PID identity/cwd before stopping them. Same private socket `/tmp/caitlyn-discord-test.uEUE6g/worker.sock`; same process-only local DB/social-enabled/AI-disabled overrides. Current source-only image: `sha256:33487c5a28b95f91eececb32e00cbf46b59d315656b00d220896fc48a3efa6f5`. No packages installed; VM intentionally left running. Old PIDs in the checkpoint below are stopped.

Validation: `npm run check` passes **221 tests**, one optional PG skip; the disposable PG suite passes **18 tests**, including complete sibling gating, partial/stale lease refusal, concurrent cleanup claims, self-delete events, and migration replay. Real isolated image/video/regression/quote worker checks all pass again. Command definitions did not change (only response copy), so the existing 14 registrations remain valid. No commit/merge/push/homeserver changes. Next: owner retests a new ordinary X post for sender mention, copied caption, delayed original deletion, and preserved replacement; sensitive examples need an age-restricted destination and must still be available from X without bypassing access controls.

### Previous: command errors, restored voice history, and media HTTP 403

The owner confirmed the homeserver instance is inactive and explicitly authorized using the existing Caitlyn bot locally, including registering new commands. Verified the bot token's application matches `CLIENT_ID` and the configured guild is MandemHQ. Registered and read back all 14 guild commands; newly published commands are `/social`, `/setup`, and `/logs`. Global commands remain unchanged (none). This was Discord command registration, not a code push/deployment to the homeserver.

Initially applied migration `014_social_delivery.sql` to verified `127.0.0.1:5432/caitlyn_test`. The owner's command/media tests then revealed three separate issues, now fixed locally:

- An additional VS Code `npm run dev` process started at 13:00:41 UTC (Node PID `83162`), alongside the assistant-started bot. Discord logs showed competing successful/failed acknowledgements and duplicate voice/birthday checks. Stopped both verified local instances and restarted one. Shared deferral/error handling now contains expired interactions and explicitly diagnoses `40060`; startup logs include the PID. There is no cross-host singleton enforcement—do not launch another bot with this token.
- Restored data contained multiple open voice sessions for three guild/user pairs. Applied migration `015` transactionally: 23 ambiguous records are flagged, not deleted or credited. Fingerprints confirm every original voice field, user/daily total, and birthday is unchanged. Future tracking excludes these records and a unique index prevents duplicate active sessions. No historical timestamp reconciliation was attempted.
- The user's two sent X previews had zero attachments and `access_denied` outcomes. Gateway diagnostics confirmed a permitted connection to the CDN; the HTTP 403 occurred during media retrieval. Using an explicit honest `CaitlynMediaWorker/1.0` User-Agent restored original image and MP4 retrieval without authentication, cookies, browser impersonation, redirects, or alternate-host fallback. The real broker/client/renderer now passes image (398,830 bytes), video (3,162,626 bytes), the user's exact failed MidPeng post (2,652,337 bytes), and quote (two clips, 1,348,582 bytes), all with zero omissions. Already-sent text-only previews are deliberately not auto-reposted.

The dedicated Colima profile is **running and intentionally left running** for the owner's test. No packages were installed; only cached image source layers changed. The current broker PID is `43361`, and bot PID is `44935` (verify identity/cwd before any stop). The broker still uses `/tmp/caitlyn-discord-test.uEUE6g/worker.sock`, private parent/socket mode 0600, and image `sha256:45576170c8d2f5032d878a7b59dbc1da32bc5916ed79847c23b4df73e0a6f2bc`. Bot startup reached `Caitlyn is online` at **13:20:29 UTC**. Process-only settings: local DB, `SOCIAL_MEDIA_ENABLED=true`, that socket, `LLM_ENABLED=false`, `LOG_LEVEL=INFO`; `.env`/repository defaults remain unchanged. Ordinary `npm run dev` still defaults to social integration off.

The owner enabled social channel `1152942687603933285` and configured private log channel `1545721255515267113` with all five log types. These settings are preserved, not hard-coded into runtime code. Post-restart log readback shows successful startup/forwarding and no warnings/errors. Idle queue DEBUG logs are throttled to one per five minutes. Reconfirmed all 14 guild commands by REST readback; no command schemas changed, so re-registration was unnecessary.

Verification: `npm run check` and `LOG_LEVEL=ERROR npm test` pass **211 tests**, with one optional PostgreSQL skip. The explicitly enabled disposable PostgreSQL suite passes **17 tests**. Five Python request-safety tests pass using the existing sandbox runtime, no network/host mounts/installations. Real local media checks removed their own containers, bytes, volumes, and temporary sockets. No homeserver access, commit, merge, push, or deployment this turn.

Next: the owner was asked to retry commands and repost the failing X link; actual Discord playback after this fix remains to be confirmed. Continue preview/edit/delete checks, and stop the exact local bot/worker/Colima when testing finishes. Do not auto-resend sent previews. Retain local test records for diagnosis. Stop/restart instructions are in [social delivery](social-delivery.md#authorized-manual-discord-test). The sections below are historical checkpoints, not current runtime/media status.

## Previous continuation: opt-in X delivery integration

1Password signing succeeded on retry: `0f3dee4 feat: add tested X post extraction and safe rendering`. No merge or push. Continued with the approved worker/queue/configuration implementation; this newer work is uncommitted.

Added an owner-only Unix worker broker, bounded validated attachment transport, migration `014`, a leased/deduplicated PostgreSQL queue, safe Discord replies and conservative uncertain-send recovery, source edit/delete cancellation, periodic preview cleanup, and administrator `/social` channel controls. Jobs use the existing guild-scoped private logging. Master integration and new channels default off. The old hosted-fixer rewrite/delete path is removed from this branch; X is the only replacement adapter wired in and other platforms now keep their original links. No real bot is running this branch.

See [social delivery](social-delivery.md) for implementation limits, worker permissions, database state, activation steps, and known races. `npm run check` passes 203 tests with one optional PostgreSQL skip; the separately enabled disposable PostgreSQL suite passes all 16 tests. Local real transport delivered the owner's 398,830-byte X PNG through the broker/client/renderer. The quote's text/attribution still resolves, but video now returns HTTP 403 (`access_denied`); subsequent media is not attempted and the result is explicitly partial. Do not repeat blocked requests or treat the September 10 video success below as current availability.

No migration was applied to the restored `caitlyn_test` database; `.env` is unchanged. No Discord login, command publication, homeserver access, merge, push, or deployment. No new packages were installed: only cached image source layers changed; exact reused dependencies and VM emulator registration are documented in [installation inventory](media-test-installations.md). Test databases, containers, downloaded media, IPC volumes, and sockets were removed; the dedicated VM was stopped. The default Docker context remains `default`.

Next: review this integration and the current X access limitation, then obtain approval for an isolated live Discord test channel (migration/worker/command activation included explicitly). Verify desktop/mobile video behavior and real permission/edit/delete handling before rollout. Production service supervision/image build and remaining Instagram/TikTok/Reddit adapters/access decisions are still open. Do not publish to `main` or deploy implicitly.

## Historical continuation: social-media replacement feasibility (2026-09-10)

**Current checkpoint:** signed the existing feasibility tools as `03eab46` after 1Password became available. No merge/push. Continued on the same feature branch with uncommitted `core/socialXPost.ts` normalization, `core/socialPostRender.ts` offline payload construction, shared contracts, and isolated X extraction/download verification (`scripts/mediaSandbox/xMetadata.py`, `xPostWorker.ts`, harness `--x-post` mode). The existing social message handler is still untouched; no live replacement is enabled.

The five owner-supplied X examples now pass normalized text/attribution and all reported image/video checks. The gallery has two original images; the quote has **two independently attributed videos**, one parent and one quoted post, both H.264/AAC. Images are decoded as single frames; clips only for two seconds. Exact results, budgets, limitations, and next steps are in [social-media replacement](social-media-replacement.md#x-adapter-and-renderer-continuation). The renderer accepts validated buffers only, disables mentions, preserves gallery/quote ordering, labels missing media, and offers full-text attachments under caller-supplied upload budgets. It makes no Discord calls.

Added 24 deterministic tests; `npm run check` passes 173 tests with one optional PostgreSQL skip. Production worker/file transport, durable delivery/deduplication, per-server opt-in controls, and live Discord rendering remain open. No database changes, bot login, command publication, homeserver access, merge, push, or deployment. No new packages were installed: only the existing image's source layer changed. Exact earlier host/container dependencies are now fully listed in [installation inventory](media-test-installations.md). Test containers/downloads/IPC volumes were removed and the dedicated VM stopped after testing. The paragraphs below retain historical checkpoints.

Local-test follow-up: the owner chose this Mac. Installed Colima 0.10.3, Docker CLI 29.8.0, and Lima 2.2.0; created a dedicated no-host-mount profile without changing the default Docker context or enabling autostart. Added a pinned disposable extractor/FFmpeg image, a public-host/IP-filtered Unix gateway, a network-none worker, and a local orchestration script under `scripts/`. Four more offline tests cover the gateway and error categories. See [local media testing](local-media-testing.md) for all limits and repeat instructions.

All live isolation checks passed. X video and quoted clips downloaded with H.264/AAC and decoded; Instagram examples yielded decodable H.264 without audio streams; the long TikTok reached the CDN but exceeded the 32 MiB test cap. Images/text/gallery completeness, quote modeling, Instagram audio, oversized videos, and Reddit approval remain open. `npm run check` now passes 149 tests with one optional database test skipped. All case containers/downloads/IPC volumes were removed and the dedicated VM stopped; installed tools/images remain reusable. No bot, database, homeserver, commit, push, or deployment changed. The paragraphs below retain the preceding metadata-only checkpoint.

The owner approved investigating a full replacement for hosted embed fixers and supplied public X/TikTok/Reddit examples. Created `codex/social-media-replacement` from `caitlyn-3.0`. Added a network-free canonical link parser, shared identities, an explicit bounded official-oEmbed diagnostic using existing logging, and 17 offline tests. The existing social message handler remains unchanged; the replacement is not enabled.

Live official-endpoint checks returned HTTP 200 embed markup for five unique owner-supplied X posts, the TikTok video, and three supplementary public Instagram examples. This does not prove media retrieval, text completeness, galleries, or quoted-post relationships; Instagram returned no author/title/thumbnail fields. Reddit API checks stop at an explicit approval gate. No original media was downloaded and no paid APIs or account cookies were used. Full results, limitations, sample links, invocation, and remaining implementation gates are in [social-media replacement](social-media-replacement.md).

`npm run check` passed typechecking, lint, a clean build, and 145 tests with one opt-in PostgreSQL test skipped. This checkpoint is uncommitted. No dependencies, migrations, database records, Discord configuration, live bot process, or homeserver were changed; nothing was pushed or deployed. Next: agree access methods/paid-API preference and an isolated worker test runtime, then verify actual images/video/audio and quoted media before building and activating delivery. No `yt-dlp`, FFmpeg/ffprobe, or Docker was found on this Mac's PATH.

## Previous continuation: birthday recovery

After approving same-day catch-up and duplicate prevention, the owner also requested use of the existing logging. Implemented startup/five-minute checks with a 9 AM-to-midnight delivery window and optional `BIRTHDAY_TIMEZONE` (host timezone by default; currently Europe/Brussels locally). Date matching uses PostgreSQL calendar dates, including February 29 only in leap years, without modifying stored birthdays.

Migration `013_birthday_delivery_tracking.sql` adds grouped delivery records and unique server/person/date reservations. Short transactions reserve recipients, conditional claims coordinate concurrent workers, and persisted backoff delays failures before sending. Messages stay grouped in batches of at most 25. A timed-out/failed send or failed acknowledgement remains claimed; bounded Discord history reconciliation can confirm the original message but cannot authorize a blind resend. Late send success is observed. Unknown delivery stays held and is logged for review. See [birthday recovery](birthday-recovery.md) for exact state, snapshot, retention, cancellation, and delivery-limit contracts.

All logs use the existing guild-scoped logger and private channel filters: DEBUG checks, INFO attempts, SUCCESS deliveries/reconciliation, WARN delays/uncertainty, and ERROR failures. `/logs levels types:info,success,warning,error` includes successful deliveries; no new slash-command publication is needed.

Verification: `npm run check` passed typechecking, lint, a clean build, and 128 tests with one opt-in database test skipped. The separately enabled disposable PostgreSQL suite passed all 14 tests, including real concurrent claims, rollback, backoff, calendar matching, and migration replay. It removed only its own synthetic test database. Migration `013` was then applied to verified local `127.0.0.1:5432/caitlyn_test` in a transaction: all ten birthday rows match their pre-migration fingerprint, and both recovery tables are empty. AI remains disabled locally; no live Discord process was started.

The feature is committed as `3bfb0dd` and merged locally, but is not pushed or deployed. Before the first deployment, stop the legacy bot and apply only missing migrations. Start before 9 AM on a day not already announced by the legacy bot, or wait for the next such day: old sends have no tracking and cannot be inferred safely. Never run old and new reminder workers together. Homeserver migration and live verification remain separately authorized release work.

## Previous continuation: Discord logging

The owner requested a checkpoint commit and merge before starting server-configurable Discord logging. After reopening 1Password, signing succeeded: `b998543 feat: harden bot failure handling and organize documentation`. `caitlyn-2.0` was fast-forwarded to that commit, and `features/discord-logging` starts from the same base. No push or change to `main` was made.

The owner clarified that only the main server should receive logs, and log types must be selectable from that channel. Replaced the experimental per-server mode with one reserved main-server destination. `/setup logs|status|disable` is bot-owner-only; disabling retains the server reservation. `/logs levels types:info,warning,error` selects exact types, with `all` and `none` shortcuts; `/logs status` shows them. These commands require ownership and administrator permission, and `/logs` only works inside the configured channel. Level selections persist and operate independently of console `LOG_LEVEL`, including debug output while the console is filtered to INFO/ERROR.

No log-channel/server IDs are hard-coded. Legacy server-scope rows are ignored, not deleted or promoted. Other servers receive no logs or logging-onboarding announcements; global publication excludes `/setup` and `/logs`. Publish those private commands only to the main guild. Forwarding retains credential redaction, bounded queues/batching, backoff, channel privacy checks, and lifecycle cleanup. The `--global` publication option remains explicit and is never part of startup.

`npm run check` passed typechecking, lint, a clean build, and 112 tests with one opt-in database test skipped. The separately enabled disposable PostgreSQL suite passed all nine tests and cleaned up its own database. Migrations `011` and `012` are applied to verified local `127.0.0.1:5432/caitlyn_test`; the settings table contains zero configurations. Migration `012` adds level selection and reserves one main server even while forwarding is disabled. Existing application records and the homeserver are unchanged. AI remains disabled locally.

Private logging was signed as `7ad30ed` and fast-forwarded into local `caitlyn-2.0`. Nothing has been pushed or deployed. No live Discord login, slash-command publication, or channel messages were sent. Remaining release steps require separate authorization: apply missing migrations to the intended deployment database, publish guild commands only to the main server, and verify `/setup logs` followed by `/logs levels` inside the private channel. See [private logging and setup](discord-logging.md) for commands, ownership, privacy, delivery limits, and remaining legacy multi-server constraints.

## Completed file conventions and documentation

Supporting project documentation is consolidated under `docs/`: the task list moved to `docs/.todo`, and private restore notes moved from `backups/README.md` to Git-ignored `docs/local-database.md` with owner-only permissions. At the owner's request, `README.md` is back at the repository root for GitHub, with links and conventions updated accordingly. Tool-discovery/configuration files remain where their tools require them.

Source, script, test, and type headers follow the existing `@file`, `@description`, and `@module` pattern, with accurate filenames and responsibilities. Keep new code in the established role-based folders; the conventions are recorded in [development instructions](development.md#file-headers). This cleanup changes comments and documentation, not runtime behavior.

Verification after cleanup: `npm run check` passed typechecking, lint, a clean build, and 88 tests (the opt-in PostgreSQL suite was skipped). All 71 JavaScript/TypeScript files have valid headers, their contents below those headers are unchanged by this cleanup, and all 66 local documentation links resolve. Private restore notes remain Git-ignored with mode `0600`. No database operations, commit, push, or deployment were performed for this cleanup.

## First improvements after the checkpoint

- Added `backups` to `.dockerignore`, complementing the Git exclusion. Local database data and private environment snapshots must not enter Docker build contexts.
- Added append-only migration `009_create_birthdays_table.sql`, matching the schema inspected in the copied database: serial primary key, bigint Discord ID, text name, and date of birth, all non-null. It creates the table only when absent and does not modify restored data. Migrations `001` through `008` are unchanged.
- Added an opt-in local PostgreSQL test covering the complete migration sequence, birthday schema, row and sequence preservation on replay, and birthday CRUD. All five tests passed against local PostgreSQL; the uniquely named test database was removed in cleanup. It used only synthetic data and did not touch the restored application database. Execution instructions are in `docs/development.md`.
- Documented that the existing migration runner has no history tracking and that replaying all historical migrations against a restored database is unsafe.

## Implemented error-handling improvements

The user authorized implementation of the application-wide error-handling audit. Startup readiness/cleanup, DB deadlines and error listeners, event containment, safe command responses, cooldowns, transactional activity writes, safe replacements, birthday date/upsert handling, AI/API resilience, and bounded shutdown are implemented. The Docker entry point now runs Node directly; command publication is separate and no longer clears global commands. See [resilience and rollout](resilience.md) for behavior contracts, tests, and remaining work; the [audit](error-handling-audit.md) retains the original findings.

The local `.env` had `LLM_ENABLED=true`; it is now `false`. The example configuration and missing-variable runtime default were already disabled. Added a regression test for the absent-variable/default-reset case. No running or remote bot was changed.

The disposable PostgreSQL suite passed all eight integration tests, including rollback at each voice write, concurrent completion counting a five-second session once, transactional message counters, and birthday uniqueness/upserts. At the resilience checkpoint, `npm run check` passed typechecking, lint, a clean ESM build, and 88 tests; the opt-in database suite was skipped during the ordinary run. The local application pool still targets `127.0.0.1:5432/caitlyn_test`, and AI remains disabled. These improvements are committed as `b998543`; nothing has been pushed.

Applied migrations `009` (no-op) and `010` to local `caitlyn_test` only, after confirming no duplicate birthdays. The unique Discord-ID index exists and all ten birthday rows/dates remain intact. No production migration or Discord publication was performed.

Historical snapshot cleanup is out of scope at the owner's request and has been removed from the active backlog. Database records and the runtime safeguards for ambiguous sessions remain unchanged. Durable birthday catch-up/deduplication remains a separate feature. Existing stored birthday dates were not rewritten.

## Completed in the modernization checkpoint

- Updated production dependencies to date-fns 4.4.0, Discord.js 14.27.0, dotenv 16.6.1, node-cron 4.6.0, and pg 8.23.0. Refreshed the transitive `ws` dependency within Discord.js's supported range. The full npm audit now reports zero vulnerabilities, down from four moderate and three high findings.
- Removed the unused `node-fetch` dependency. All HTTP callers already used Node.js fetch, and their existing behavior tests continue to pass.
- Removed `@types/node-cron`; v4 ships declarations. Birthday jobs still run at `0 9 * * *` in the host's local timezone. Their callbacks return the reminder promise so cron observes asynchronous completion and failure. A test exercises the actual v4 scheduler with a fake reminder and a fixed clock.
- Added `core/environment.ts`. Startup validates settings before creating the Discord client, contacting PostgreSQL, registering jobs, or logging in. Errors name every missing or malformed setting without printing its value. AI configuration is required even with AI initially disabled because `/toggleai` can enable it at runtime. Context counts default to five recent and three similar messages; zero is supported. See the [README](../README.md#configuration) and `.env.example` for the complete configuration contract.
- Moved Discord token validation from module import to login. Importing the application without credentials is safe. Command deployment rejects blank credentials and exits with status 1 on failure, allowing the Docker startup command to stop when deployment fails.
- Fixed four tests that depended on ambient log filtering by mocking logger methods. Kept separate tests for logger output and severity filtering. Compiled command/event discovery now runs with reversed filesystem enumeration and compares event membership without assuming directory order.

## Modernization checkpoint verification

- `npm run check`: strict typecheck, lint, clean ESM build, and all 70 tests passed.
- `LOG_LEVEL=ERROR npm test -- --test-reporter=dot` and `LOG_LEVEL=DEBUG npm test -- --test-reporter=dot`: all 70 tests passed under both settings.
- `npm audit --json`: zero vulnerabilities across production and development dependencies.
- `npm ls --depth=0`: the installed tree satisfies the manifest; the TypeScript 7 compiler / TypeScript 6 lint API arrangement is preserved.
- `git diff --check`: clean. No generated `dist` files are tracked.
- Tests use substitutes for Discord, PostgreSQL, AI endpoints, and Giphy. Compiled startup and command-deployment tests verify failure without credentials from isolated temporary directories.

## Remaining work

1. Verify the Docker image on a host with Docker installed. `command -v docker` exited with status 1 on this Mac; no local image build was possible. Run `docker build --tag caitlyn:2.0-integration .` on a Docker-enabled host.
2. Choose the license. `package.json` still says ISC and README still says MIT; there is no project license file. The choice remains with the owner.
3. Handle birthday recovery's first deployment and private logging publication in a separately authorized release; see [birthday rollout](birthday-recovery.md#rollout-and-verification).

No push, command registration against Discord, image publication, workflow trigger, or homeserver update was performed during this continuation. Publishing or promoting the branch remains a separate release decision. Future feature branches use `caitlyn-3.0` as their base.

## Local database snapshot

On 2026-09-05, copied the production `caitlyn` database over Tailscale using the homeserver's PostgreSQL 17.8 tools. The verified custom-format dump is in the Git-ignored `backups/` directory alongside its archive listing and checksum metadata. Private restore instructions are in Git-ignored `docs/local-database.md`. The dump contains all five `discord` tables, including the birthday table absent from the historical migration set.

The source database was only read. The backup contains application data and must remain outside Git.

On 2026-09-05, installed PostgreSQL 17.11 and pgvector 0.8.6 through Homebrew, started the local service, and restored the snapshot into `caitlyn_test` at `127.0.0.1:5432` as the local role `raze`. The service listens on localhost and starts at login. Registered and connected `Caitlyn Local` in the existing pgAdmin app. All five table counts match the archive and all indexes are valid; details and service commands are in Git-ignored `docs/local-database.md`.

The project's `.env` now uses `PGHOST=127.0.0.1`, `PGPORT=5432`, `PGDATABASE=caitlyn_test`, and `PGUSER=raze`. `PGPASSWORD=[REDACTED: use the private local .env]` is a non-secret placeholder required by startup validation; local trust authentication does not use it. Verified the connection through the application's PostgreSQL pool and confirmed all five restored tables. The previous environment is saved privately in `backups/env-before-local-2026-09-05T09-20-34.859Z.env`. Other application settings were preserved.
